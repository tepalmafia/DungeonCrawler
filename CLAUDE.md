# 이 저장소에서 일할 때

## 목적 — 한 줄

**시간 가는 줄 모르고 계속 도는 게임.** (`web/3d` — 3D 액션 RPG)

이 한 줄이 모든 판단의 기준입니다. 어떤 변경이든 「이게 계속 돌게 만드는가」로
답이 갈립니다. 자세한 것은 `docs/GRIND.md`.

> **주의 — 목적이 한 번 바뀌었습니다.** 예전 목적은 「한 번 끝까지 가는 짧고
> 밀도 높은 여정」이었고, 코드 주석과 옛 문서에 그 흔적이 남아 있습니다.
> 「드랍 빈도는 줄인다」 · 「한 마리씩 끌어와 오래 싸운다」 · 「판이 끝나면
> 사라진다」 같은 문장을 만나면 **그건 낡은 목적입니다.** 뒤집힌 목록은
> `docs/GRIND.md §7` 에 전부 적혀 있습니다.

## 목적을 숫자로 — 이게 진짜 기준입니다

「시간 가는 줄 모른다」는 감상이라 검증할 수 없습니다. 잴 수 있는 형태로 못박아
두고, **바꿀 때마다 잽니다.**

```
node tools/sim.js
```

| 지표 | 목표 |
|---|---|
| 한 마리 잡는 시간 | 1.5 ~ 3초 |
| 처치 사이 간격 | 3.5초 이하 |
| 드랍 사이 간격 | 30 ~ 60초 |
| 층 하나 | 3 ~ 5분 |
| 회차 하나 | 15 ~ 25분 |

시뮬이 ✔ / ✘ 로 찍습니다. **✘ 가 하나라도 있으면 목적에서 멀어진 것**이고,
그 숫자를 근거로 고칩니다. 느낌으로 조정하지 않습니다.

## 숫자는 한 곳에만 둡니다

밸런스 값을 **코드에 흩어 놓지 않습니다.** 표에 두고 게임과 시뮬이 같은 표를
읽습니다. 두 곳에 적으면 반드시 갈라집니다 (`?v=3` 과 `VERSION` 이 스무 번
배포되는 동안 어긋나 있던 사고가 그 예입니다).

| 표 | 무엇 |
|---|---|
| `game/item-table.js` | 등급·기반·접사·롤·합산 |
| `game/enemy-table.js` | 종족·변종 수치 · 밀도(DENSITY) |
| `game/growth-table.js` | 레벨 성장식 · 방어도 감쇠 · 경험치 곡선 · 회차 배수 |
| `game/economy-table.js` | 드랍 확률 · 조각 · 사망 · 회차 해금 |
| `game/skill-table.js` | 스킬 마나·쿨·피해 배율 |
| `world/floors.js` | 층별 배수 · 로스터 · 속성 분포 |

**이 표들은 three.js 를 안 씁니다.** 그래야 `tools/sim.js` 가 브라우저 없이
읽습니다. 새 숫자를 넣을 때 three 를 끌어오면 그 순간 시뮬이 못 읽습니다.

## 작업 방식

- **고친 것만 잽니다.** 기능 하나를 고치면 그것만 재는 스크립트를 씁니다
  (30초~2분). 전체 검사를 매번 돌리지 않습니다
- **화면에 보이는 것은 화면으로 확인합니다.** UI 를 고쳤으면 스크린샷을 찍어
  같이 보여 줍니다. 「창이 화면 밖에 그려지고 있었다」를 코드로는 못 찾습니다
- **배포**: `claude/3d-rpg-game-sample-6dewee` 브랜치 → PR → main 머지 →
  GitHub Pages 자동 배포
- **캐시**: `web/3d/index.html` 의 `?v=` 와 `js/main.js` 의 `VERSION` 은
  **항상 같이** 올립니다. 어긋나면 브라우저가 옛 파일을 계속 씁니다

## 도구

```
python3 tools/serve.py 8137      로컬 서버 (게임을 열려면 필요)
node tools/sim.js                밸런스·템포 (1초)
node tools/sim.js --csv          엑셀용
node tools/bump-version.js N     캐시버스트 동기화
```

감사·회귀 스크립트는 폐기했습니다. 실제로 버그를 찾은 것은 늘 **직접 돌려
보고 나온 보고**였고, 13분짜리 전체 검사는 그걸 못 잡았습니다.

## 글은 한국어로

주석·문서·커밋 메시지 전부 한국어입니다. **무엇을 했는지가 아니라 왜 그렇게
했는지**를 적습니다. 특히 **틀렸다가 고친 것**은 반드시 남깁니다 — 같은 함정을
다음에 또 밟습니다.
