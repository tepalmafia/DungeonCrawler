// ══════════════════════════════════════════════════════════════════════════
//  추격 — **2단계의 전부다.**
//
//  ★ 이 단계가 판가름이다 (docs/space/PLAN.md §13)
//    「접촉 → 자국이 오른다 → 전력을 옮긴다 → 뿌리친다」 하나만 돈다.
//    채굴도 거점도 식량도 없다. **이게 재미없으면 §7 을 통째로 다시 짠다** —
//    방을 다 꾸민 뒤에 알면 늦다.
//
//  ★ 숫자는 전부 **초안**이다. 잰 것이 아니다.
//    PLAN §11 의 목표(추격 90초~3분 · 방 이동 3~6회)에 맞추는 것이 다음 일이고,
//    그때 시뮬이 ✔/✘ 를 찍는다. 임시라고 여기 적어 두지 않으면 6개월 뒤에
//    「근거 있는 값」으로 둔갑한다.
//
//  ★ three.js 를 쓰지 않는다 — 시뮬이 브라우저 없이 읽어야 한다.
// ══════════════════════════════════════════════════════════════════════════

/**
 * 전력 — **하나인데 쓸 곳이 셋이다** (PLAN §7-0 축①).
 *
 * 셋 중 **둘만** 켤 수 있다. 이 하나가 도망을 「W 누르고 있기」에서
 * 「무엇을 포기하는가」로 바꾼다. 그리고 메뉴가 아니라 **통로의 차단기를
 * 손으로 내리고 올려서** 한다 — 그래야 방 사이를 오가는 것이 비용이 된다.
 */
export const CIRCUITS = [
  { key: 'thrust', name: '추진', off: '못 달린다' },
  { key: 'cool', name: '냉각', off: '열이 안 옮겨진다' },
  // ★★ 「센서」였는데 **한 스위치가 두 물건을 하고 있었다** (REAL.md §2-G).
  //   「끄면 안 보인다」는 수동 센서고, 「전파를 쏘니 티가 난다」는 능동
  //   레이더다. 둘은 다른 물건이다. **수동 청취는 늘 켜져 있고**(그래서
  //   있고 없고는 안다), 끄고 켜는 것은 **능동 탐지**다 — 끄면 안 보이는
  //   것이 아니라 **거리를 못 잰다.**
  { key: 'sensor', name: '능동 탐지', off: '거리를 못 잰다' },
];

/**
 * ★★ **없앴다** (v58 · 사장님 「냉각을 켜면 전력이 모자라서 센서를 못 키는게
 *   말이 돼?」). 3 이므로 셋 다 켜진다 — 이 상수는 옛 검사가 읽고 있어서
 *   남겨 두었을 뿐, 이제 아무것도 안 막는다.
 *
 *   ★ 사장님이 맞았고 내가 생각한 것보다 나빴다. 소비 전력은 **자릿수가
 *     다르다** — 주 추진 수십 MW · 냉각 펌프 수 kW · 센서 수신기 수백 W.
 *     **전구를 켜면 엔진이 꺼지는 자동차**였다. 게다가 방향까지 반대라,
 *     추진이 열을 제일 많이 내는데 규칙이 바로 그때 냉각을 막았다.
 *
 *   ★ 그럼 무엇이 묶나 — **열이다** (`heat-table.js`).
 *     다 켜면 저장고가 빨리 차고, 차면 라디에이터를 열어야 하고,
 *     열면 자국이 폭발한다. **긴장은 그대로인데 이유가 생겼다.**
 */
export const POWER_MAX = 3;

/**
 * 자국 — 얼마나 눈에 띄나. **판단의 근거라 숫자로 보여준다** (PLAN §6 ★).
 *
 * 축② 「빠르면 잘 보인다」가 여기 들어 있다 — 추진을 켜면 자국이 확 오른다.
 * 무작정 밟는 것이 최선이면 그건 전략이 아니다.
 */
export const SIGN = {
  base: 10,
  perHeat: 0.42,     // 열 1당. 열을 안 잡으면 그것만으로 눈에 띈다
  thrust: 30,        // 추진을 켜면 이만큼 더
  cool: -9,          // 냉각을 켜면 이만큼 덜 (열이 내려가는 것과 별개로)
  /**
   * ★★ **5 였다 — 너무 쌌다** (v58 · REAL.md §2-G).
   *   능동 레이더는 **등대를 켜는 것**이다. 내가 쏜 전파가 되돌아오는
   *   동안 상대에게는 나를 가리키는 화살표가 된다 — 그런데 추진(30)의
   *   6분의 1 이라 「그냥 켜 두는」 것이 늘 답이었다.
   *   20 이면 **켤 때마다 고민이 된다**: 거리를 알 것인가, 안 들킬 것인가.
   */
  sensor: 20,
  /**
   * ★ **밸브를 열어 두면 자국이 커진다** — 방열은 곧 적외선 신호다.
   *
   *   `docs/space/CHASE2.md` 에 적어 둔 「우주에서 스텔스는 불가능하다」와
   *   **같은 규칙**이다. 열을 버리는 것은 곧 나를 밝히는 것이다.
   *
   *   이게 없으면 밸브를 「열어 놓고 잊는」 것이 공짜가 되고, 그러면
   *   냉각이 계통이 아니라 스위치가 된다. 있으면 **「언제 열고 언제
   *   잠글까」**가 판단이 된다 — 열이 높을 땐 열어야 이득이고
   *   (열이 빨리 내려가 자국이 준다), 낮아지면 잠가야 이득이다.
   */
  valveOpen: 17,
  max: 100,
  /** 이 값을 넘긴 채로 오래 있으면 붙는다 */
  contactAt: 52,
  /** 넘긴 동안 초당 쌓이는 위험. 100 이 되면 접촉 */
  riskRise: 3.4,
  /** 밑돌 때 초당 빠지는 위험 */
  riskFall: 2.2,
};

/**
 * 추격 — 거리는 0~100 이다. 100 이면 뿌리친 것, 0 이면 잡힌 것.
 *
 * ★ 「잡힌다」가 게임 오버가 아니다 (PLAN §4-4). 2단계에는 아직 나포도
 *   거점도 없으므로, 지금은 **잡히면 그냥 다시 시작**한다.
 */
export const CHASE = {
  startDist: 44,
  escapeAt: 100,
  // ★ 처음 넣은 값(10.5 / 1.6 / 0.17)은 **시뮬이 바로 걷어냈다.**
  //   추격이 14초 만에 끝났다 — 목표는 90~180초다. 벌어지는 속도와 붙는
  //   속도가 거의 **비기게** 잡아야 그 길이가 나온다. 감으로는 못 맞춘다.
  //   3.4 로는 73초, 2.6 으로는 236초였다. 2.95 가 그 사이다. **뜨거우면 지고 식히면 이기는**
  //   모양이 된다 — 열 45 쯤에서 벌어지는 속도와 붙는 속도가 뒤집힌다.
  //   그게 「기관실에 다녀올 시간이 있나」라는 질문을 만든다.
  thrustGain: 2.95,   // 추진 ON 일 때 초당 벌어지는 거리
  drift: 0.35,        // 추진이 꺼져 있어도 관성으로 조금은 간다
  trackBase: 0.5,     // 상대가 붙는 기본 속도
  trackPerSign: 0.055, // 자국 1당 더 붙는다. 자국 53 이면 딱 비긴다
  /**
   * 뿌리친 뒤 이만큼은 아무 일도 안 난다 — 숨 돌릴 틈 (USER-VIEW §3-6).
   *
   * ★ 12초였다가 60초로 올렸다. PLAN §11 은 처음부터 「안도 구간 60초
   *   이상」이라고 적어 뒀는데 **표는 12 였다** — 2단계에서는 추격이 한 번만
   *   돌아서 아무도 못 봤다. 항로가 생겨 한 회차를 통째로 재 보니 구간
   *   하나에 추격이 **네다섯 번** 붙었다. 뿌리치자마자 또 붙으면 그건
   *   긴장이 아니라 소음이다.
   */
  calmAfter: 60,
  /** 접촉 직후, 아직 아무것도 안 했는데 급해지지는 않게 */
  graceAfterContact: 2.5,
};

/**
 * 잡히면 — **죽지 않는다. 뺏기고 일이 는다** (PLAN §4-4).
 *
 * ★ v21 까지 이게 **아예 없었다.** `PHASE.CAUGHT` 는 `stepChase` 에 분기조차
 *   없는 종착 상태라, 잡히면 게임이 **위협이 영구히 사라진 빈 상자**가 됐다 —
 *   경보도 꺼지고 소리도 평온으로 돌아가고, 압박이 넘쳐도 안 붙었다.
 *   위 53행이 「잡히면 그냥 다시 시작한다」라고 적어 뒀는데 **적어만 뒀다.**
 *   사장님이 「아무것도 못하고 그냥 끝나는데」라고 하신 게 이것이다.
 *
 * ★ 벌이 **일**이지 숫자가 아니다 — 충돌(HAZARD.hit)과 같은 결이다.
 *   그리고 **식량·부품은 안 뺏는다.** 그것까지 가져가면 회복 불능이 된다.
 */
export const CAUGHT = {
  /** 실려 있던 광석을 전부 — 「한 통만 더」의 값이 여기서 매겨진다 */
  ore: 1.0,
  /** 고장 몇 개를 두고 가나 */
  faults: 2,
  /** 선체 마모 — 다음 고장이 선체 쪽으로 쏠린다 */
  hull: 0.30,
  /** 배를 뒤지는 동안 (초). 이 사이에 화면·소리가 「털린다」 */
  hold: 4.0,
  /**
   * 놓아준 뒤 이만큼은 다시 안 붙는다 — 털리자마자 또 붙으면 배울 게 없다.
   * ★ 45 로 뒀다가 60(= calmAfter)으로 올렸다. `space-route.js` 가
   *   **구간당 추격 4회**를 찍었기 때문이다 — 잡히면 고장 둘이 새로 열리는데
   *   그걸 고치기도 전에 또 붙으면 스스로 무너진다. 그리고 애초에 짧을 이유가
   *   없었다: 상대는 원하던 것을 가져갔으니 더 쫓을 까닭이 없다.
   *   **뿌리쳤을 때와 같은 안도를 준다** — 벌은 뺏긴 것과 늘어난 일이지,
   *   숨 돌릴 틈을 뺏는 것이 아니다.
   */
  calm: 60,
};

/**
 * ★★ **열이 나는 곳** — 다시 잡았다 (v58 · docs/space/REAL.md §2-B).
 *
 * ★ 여기는 이제 **「열이 생기는 양」만** 적는다. 열이 **없어지는 것**은
 *   `heat-table.js` 의 라디에이터가 맡는다 — 예전에는 이 표에
 *   `coolOnly: -1.4` 가 있어서 **밸브가 닫혀 있어도 냉각만 켜면 열이
 *   내려갔다.** 진공에서는 그럴 수 없다. 열을 버리는 길은 복사뿐이고,
 *   냉각 회로는 열을 **옮길 뿐**이다.
 *
 * ★ 자릿수도 셋 틀려 있었다. 잰 값 (REAL.md §1):
 *
 *   | | 예전 | 과열까지 | 지금 | 과열까지 |
 *   |---|---|---|---|---|
 *   | 다 꺼짐 | +0.5 | **132초** | +0.06 | 18분 |
 *   | 추진만 | +5.1 | **13초** | +1.96 | 34초 |
 *
 *   13초 밟으면 녹는 엔진은 엔진이 아니고, 아무것도 안 하는 배가 2분
 *   만에 녹으면 그 배는 항구에 세워 둬도 녹는다.
 *
 * ★ 지금 값이 지키는 것 (`tools/space-heat.js` 가 잰다):
 *     · 다 꺼 놓으면 **15분 이상** 버틴다
 *     · 추진만 켜면 **30~60초**에 과열 — 냉각을 켜야만 밟을 수 있다
 *     · `SINK.moveRate`(2.1)가 추진 열(1.96)을 **겨우** 이긴다.
 *       겨우여야 한다 — 넉넉하면 냉각을 켜 두는 것으로 끝나고,
 *       모자라면 냉각이 장식이 된다
 */
export const HEATING = {
  idle: 0.06,         // 배가 켜져 있기만 해도 — 아주 조금
  thrust: 1.90,       // 밟으면 여기서 난다. 열의 97% 가 이것이다

  /**
   * ★★ **정전 — 잔열** (v58 · `tools/space-blackout.js` 가 구멍을 찾아냈다).
   *
   *   `idle` 을 0.5 에서 0.06 으로 내리니 **정전(E 장면)에 시계가 사라졌다.**
   *   예전에는 「다 꺼지면 +0.5/초 → 132초에 과열」이 그대로 정전의 시계였는데,
   *   그 0.5 는 애초에 **자릿수가 틀린 값**이었다 (아무것도 안 하는 배가
   *   2분 만에 녹는다). 옳게 고쳤더니 정전이 **아프지 않은 것**이 됐다.
   *
   *   ★ 그런데 진짜 답이 물리 쪽에 있었다 — **냉각 펌프가 멎기 때문이다.**
   *     반응로는 꺼도 한동안 **잔열**을 낸다. 펌프가 돌 때는 그게 냉각
   *     회로로 빠지지만, 전력이 나가면 **갈 데가 없어 선체로 온다.**
   *     원자로가 전력을 잃으면 위험한 이유가 정확히 이것이다.
   *
   *   그래서 정전은 **0.06 + 0.44 = 0.5/초 → 132초**가 되어 대응 박자
   *   (120~240초)와 다시 겹친다. **숫자는 예전과 같은데 이유가 생겼다** —
   *   이 판이 하는 일이 처음부터 그것이다.
   */
  blackout: 0.44,
};
