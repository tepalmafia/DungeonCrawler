// ══════════════════════════════════════════════════════════════════════════
//  계통 숫자 — **여기 말고 다른 데 적지 않는다.**
//
//  게임과 시뮬(`tools/`)이 같은 표를 읽어야 하므로 **three.js 를 안 쓴다.**
//  여기에 three 를 끌어오는 순간 node 가 못 읽고, 그러면 밸런스를 감으로
//  맞추게 된다 (CLAUDE.md 「숫자는 한 곳에만 둡니다」).
//
//  ★ 지금 든 숫자는 전부 **1단계용 임시**다. 재 본 것이 아니다.
//    docs/space/PLAN.md §11 의 목표를 시뮬로 찍기 시작하면 그때 실제 값이
//    들어온다. 임시라는 것을 여기 적어 두지 않으면 6개월 뒤에 이 숫자가
//    「근거 있는 값」으로 둔갑한다.
// ══════════════════════════════════════════════════════════════════════════

/**
 * 사람 — 걷기·눈높이·팔 길이
 *
 * ★★ **주석이 거짓말을 하고 있었다** (2026-08-07 · REAL.md §2-E).
 *   `speed: 2.9` 옆에 「**뛰지 않는다** — 이 배는 좁다」라고 적혀 있었는데
 *   2.9 m/s 는 **이미 조깅**이다 (사람 걸음 1.3~1.5). 그 위에 달리기가
 *   붙어 4.6 m/s — 100m 를 21초에, 폭 2.6m 통로에서.
 *
 *   **숫자는 안 내린다.** 1.4 로 내리면 방 사이가 6~11초에서 12~22초가 되어
 *   템포가 통째로 무너진다 (`space-run.js` 가 잰 값). 대신 **말이 되게
 *   만든다** — 이 배는 `gravity` 만큼의 회전 구획이다. 저중력에서 사람은
 *   성큼성큼 뛰듯 걷고, 그게 2.9 m/s 다. 아폴로 영상의 걸음이 그것이다.
 *
 *   ★ **고친 것은 숫자가 아니라 이름과 이유다.** 이것이 §0 의 ①거짓말을
 *     ③일부러 접은 것으로 옮기는 방법이다 — 접되 **적어 둔다** (REAL.md §8).
 */
export const BODY = {
  /**
   * ★ **회전 구획의 중력** (g). 여기 있는 걸음·달리기 숫자가 지구 값이
   *   아닌 **유일한 이유**다. 0.4g 는 달(0.17)과 화성(0.38) 사이 —
   *   `space-real.js` 가 「빠르면 왜 빠른지가 표에 있어야 한다」를 묻는다.
   *
   *   ★ 화면에 회전이 안 보이는 것은 **일부러 접은 것**이다 (REAL.md §8).
   *     창밖이 안 도는 배에서 0.4g 를 말로만 주장하는 셈인데,
   *     창을 돌리면 2시간 동안 멀미가 난다
   */
  gravity: 0.4,
  speed: 2.9,        // m/s. 보통 걸음 — **0.4g 라 성큼성큼 뛰듯 걷는다**
  accel: 14,         // 미끄러지지 않게. 배 안이라 발이 바닥에 붙어 있어야 한다
  eye: 1.62,         // 눈높이
  radius: 0.34,      // 벽에 안 박히게
  reach: 2.3,        // 손이 닿는 거리
};

/**
 * ★★ **잡으면 화면이 당겨진다** (v59 · 사장님 「조정석을 잡을 때는 스크린
 *   화면을 확대해서 몰입하게 할 수 있도록 해줘」).
 *
 * ★ 왜 이게 몰입을 만드나. 이 게임의 계기는 **작다** — 실제 크기로 콘솔에
 *   박혀 있으므로 서서 보면 글씨가 안 읽힌다. 그래서 지금까지 계기를
 *   「보는」 것이 아니라 「있는 줄 아는」 것이었다.
 *   손잡이를 잡으면 **몸이 앞으로 기울고 눈이 좁아진다** — 사람이 계기를
 *   들여다볼 때 실제로 하는 일이고, 그 순간만 글씨가 읽힌다.
 *
 * ★ **UI 를 안 늘린다.** 화면에 확대창을 띄우는 것이 아니라 **카메라가
 *   움직이는 것**이다 — 「손이 곧 상태창」(PLAN §5-2)을 안 깬다.
 *
 * @property fov   잡았을 때의 화각 (평소 72). 낮을수록 당겨진다.
 *                 **45 아래로 안 내린다** — 주변이 안 보이면 1인칭에서
 *                 방향을 잃고, 그러면 놓았을 때 어디를 보고 있었는지 모른다
 * @property lean  눈이 보는 쪽으로 이만큼 나아간다 (m). 몸을 기울이는 것
 * @property rate  갈아타는 빠르기. 툭 바뀌면 멀미가 난다
 */
export const FOCUS = {
  fov: 50,
  lean: 0.34,
  rate: 3.4,
};

/** 열 — 1단계에서 도는 유일한 계통 */
export const HEAT = {
  start: 34,
  max: 100,
  warn: 62,          // 이 위로는 벽이 달아오른다
  /** 냉각이 막혀 있을 때 초당 오르는 양 */
  rise: 1.9,
  /** 냉각이 열렸을 때 초당 내려가는 양 */
  fall: 7.4,
};

/**
 * 밸브 — **끝까지 돌리는 데 시간이 든다.**
 *
 * 이게 이 게임 손맛의 핵심이다. 누르면 즉시 열리는 스위치로 만들면
 * 「E 눌러 상호작용」과 같아지고, 그러면 손이 나올 이유가 없다
 * (docs/space/PLAN.md §8).
 */
export const VALVE = {
  turnTime: 2.4,     // 0 → 1 까지 잡고 있어야 하는 초
  /**
   * 손을 떼면 초당 이만큼 되돌아온다 — 뻑뻑하다.
   *
   * ★ **0.55 였다.** 그러면 조준이 한 프레임만 흔들려도 진행이 날아가고,
   *   2.4초를 **완벽하게 겨눈 채** 버텨야 한다. 그런데 이 게임은 추격 중에
   *   화면이 흔들리고 굶으면 손도 떨린다 — 흔들리게 만들어 놓고 흔들리면
   *   벌을 주는 셈이었다. 사장님이 「마우스를 계속 누르고 있어야하지」라고
   *   하신 것의 절반이 이것이다.
   */
  slip: 0.18,
  /**
   * ★ 조준이 잠깐 벗어나도 이만큼은 **봐준다** (초).
   *   손이 미끄러진 것과 손을 뗀 것은 다르다.
   */
  grace: 0.45,
  openAt: 0.99,      // **끝까지** 돌려야 열린다. 아슬아슬하게는 안 된다

  /**
   * ★ **26초 걸렸다가, 이제는 잠글 때까지 열려 있다** (2026-08-05).
   *
   *   26초는 「기관실에 다녀와서 냉각 시간을 벌어 둔다」는 뜻이었다.
   *   그런데 밸브가 열려도 열이 초당 0.3밖에 안 내려갔으므로, 26초에
   *   버는 것이 **열 7.8**뿐이었다. 왕복 20초에 74가 오르는 배에서
   *   7.8을 벌자고 다녀오는 것은 계산이 아니라 **쳇바퀴**다.
   *
   *   이제 **열면 잠글 때까지 열려 있다.** 26초마다 돌아가는 일이
   *   사라지고, 대신 **열어 두면 자국이 커진다** (`SIGN.valveOpen`) —
   *   방열은 곧 적외선 신호다. 그래서 「열어 놓고 잊기」도 답이 아니다.
   *
   *   **잠그는 것도 같은 손짓**이다. 열린 밸브를 다시 끝까지 돌리면 닫힌다.
   */
  latching: true,
};

/**
 * **언제 열고 언제 잠그나** — 게임과 시뮬이 같은 판단을 쓴다.
 *
 * ★ 표에 두는 이유. 밸브가 걸림(26초)에서 잠금식으로 바뀌면서 시뮬 다섯이
 *   전부 「26초마다 다시 돌린다」를 흉내 내고 있었다. 각자 고치면 다섯이
 *   조금씩 다른 사람을 흉내 내게 된다 — 판단은 한 곳에 둔다.
 *
 * ★ 규칙은 한 줄이다 — **엔진을 밟는 동안은 열어 둔다.**
 *   추진이 켜져 있으면 열이 계속 오르므로 밸브를 잠글 이유가 없다.
 *   발을 떼면 냉각 회로만으로도 열이 내려가므로, 그때는 잠가서 자국을 줄인다.
 *
 *   처음엔 「열이 높으면 열고 낮으면 잠근다」로 뒀는데, 그러면 추진을 켠
 *   채로 밸브가 닫혔다 열렸다 하면서 **아무도 못 뿌리치는 배**가 됐다.
 *   시뮬이 「뿌리칠 수 있는 길 0/5」로 그 자리에서 잡았다.
 */
export const wantValve = (heat, thrust) => thrust || heat > HEAT.warn;

/**
 * 순항 — **배는 늘 가고 있다.**
 *
 * 「목적지 없이 항해한다」가 전제인데 창밖이 정지 화면이면 그 전제가 통째로
 * 무너진다. 별을 아무리 많이 뿌려도 안 움직이면 벽지다.
 *
 * ★ 이 숫자는 「진짜 우주선 속도」가 아니라 **눈에 그럴듯한 속도**다.
 *   실제 값을 쓰면 별이 안 움직이거나 순간이동한다. 창밖 별의 흐름이
 *   보기 좋은 값으로 잡고, 나중에 추진 계통이 붙으면 배수로 곱한다.
 */
export const CRUISE = {
  speed: 26,        // 초당 몇 유닛으로 별이 흐르나
  boost: 2.6,       // 밀어붙일 때 배수 (§7-1) — 아직 안 쓴다

  // ══ ★★★ v131 — **떨림은 여기서 나갔다** (`game/shake-table.js`) ═══
  //  ★ `shake 0.0035 · shakeHz 11.3 · sway 0.011 · swayHz 0.37` 이 있었다.
  //    사장님 「조준경이 빠르게 떨리는데 … 눈 아프다」로 잡히셨고, 재 보니
  //    **초당 11.3번 10화소**였다 — 진동이 아니라 깜빡임이다.
  //  ★★ 숫자만 고치지 않고 **자리를 옮겼다.** 떨림은 「배의 순항 성능」이
  //    아니라 **눈에 보이는 모양**이고, 그래야 검사가 화소로 물을 수 있다
};

/**
 * ★ 마모 — **천천히 닳는 것** (PLAN §6-2 「마모는 천천히, 사고는 급하게」).
 *
 *   이게 정비실 진단대의 존재 이유다. 조종석은 **지금**을 보여주고(열·자국·거리),
 *   관측실은 **어디로 가나**를 보여주고, 정비실은 **다음에 무엇이 터지나**를
 *   보여준다. 셋이 같은 것을 보여주면 방이 셋일 이유가 없다 (GAP.md §3-C).
 *
 *   ★ 자리는 절대 안 알려준다. 자리는 귀가 찾는다 (game/fault.js).
 *     계통을 알려주는 것과 방을 알려주는 것은 다르다 — 전자는 「무엇이
 *     닳았나」고 후자는 답이다.
 *
 *   ★ 쓰는 대로 닳는다. 밟으면 선체가, 밸브를 열면 냉각이, 차단기를 만지면
 *     전력이 닳는다. 그래서 **내가 어떻게 몰았는지가 다음 고장을 정한다** —
 *     무작위로 터지면 대비할 방법이 없고, 대비할 수 없으면 진단대는 장식이다.
 */
export const WEAR = {
  keys: ['power', 'cool', 'hull'],
  name: { power: '전력', cool: '냉각', hull: '선체' },
  /** 초당 닳는 양 */
  // ★ 처음 잡은 값이 **네 배쯤 빨랐다.** 10분만 밟아도 선체가 100% 여서
  //   구간(8~12분)마다 꽉 차 버렸고, 그러면 경고선이 늘 넘어 있어서
  //   진단대가 **아무것도 예고하지 못한다.** 늘 빨간 계기는 안 보는 계기다.
  //   지금은 한 가지만 계속 해도 20분쯤에 경고선에 닿는다.
  rate: {
    base: 0.0001,           // 그냥 켜 두기만 해도
    thrust: 0.00042,        // 추진을 켜고 달리면 선체가
    coolOn: 0.0003,         // 냉각 회로를 켜 두면
    valve: 0.00058,         // 밸브를 열어 두면 더
    debris: 0.0005,         // 잔해밭을 지나면 선체가 — 「부딪힌다」의 실체
  },
  /** 차단기를 한 번 만질 때마다 */
  perFlip: 0.004,
  /** 고치면 그 계통이 이만큼 준다 */
  relief: 0.34,
  /** 마모가 1 인 계통은 0 인 계통보다 이만큼 더 잘 터진다 */
  bias: 2.4,
  /** 이 위로는 진단대가 붉게 경고한다 */
  warn: 0.62,
};
