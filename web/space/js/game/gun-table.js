// ══════════════════════════════════════════════════════════════════════════
//  주포 — **조종석 위로 올라간다** (2026-08-05 · 사장님 요청)
//
//  ★ 먼저 밝혀 둘 것 — 내가 「안 한다」고 적어 뒀던 것이다
//    `CHASE2.md §5` 에 이렇게 적혀 있다:
//
//      | 안 한다 | 왜 |
//      | **미사일·함포** | 정비공이 아니라 함장이 된다 |
//
//    사장님이 「주포도 만들어서 적에게 대응 사격을 하고」라고 하셨으므로
//    **만든다.** 다만 그때 걱정했던 것이 사라지는 것은 아니므로,
//    **이 배의 문법으로** 만든다 — 그게 아래 셋이다.
//
//  ★ ① **탄약이 곧 수리 재료다**
//    쏘는 것은 광석과 부품이다 (`supply.ore` · `supply.parts`).
//    **쏘면 못 고친다.** 이 게임의 벌은 늘 숫자가 아니라 **일**이었고
//    (PLAN §4-4), 주포도 같은 규약을 따른다. 탄약을 따로 만들면
//    「탄약을 모으는 일」이 하나 더 느는 것뿐이고, 그건 계통이 아니라 짐이다.
//
//  ★ ② **쏘면 더 잘 보인다**
//    총구 섬광과 사출은 숨길 수 없다 (`CHASE2.md §2-1` 「우주에서 스텔스는
//    불가능하다」). 쏘면 **지금은 벌어지고 이따가 더 붙는다.**
//    그래서 「쏘고 도망」이 늘 옳은 답이 되지 않는다.
//
//  ★ ③ **올라가 있는 동안 배를 못 고친다**
//    사다리를 오르내리는 데 시간이 든다. 포탑에 있으면 밸브도 차단기도
//    패널도 못 만진다 — **동시에 두 곳에 못 있는다**는 이 게임의 축이
//    여기서도 그대로 돈다. 포탑은 「또 하나의 방」이지 새 게임이 아니다.
//
//  ★ three.js 를 안 쓴다 — tools/space-gun.js 가 브라우저 없이 읽는다.
// ══════════════════════════════════════════════════════════════════════════

// ══════════════════════════════════════════════════════════════════════════
//  ★★ v49 에서 **밖에서 안으로 들어왔다** (2026-08-06 · 사장님)
//
//    「주포는 왜 우주에 덩그러니 있어? 실내에 조준석에서 조준해야지.
//      손으로 손잡이를 잡고 wasd로 조준하는 것으로. 조종석, 주포 둘 다.
//      어떻게 디자인할지 인터넷에서 참조해서 만들어」
//
//  ★ 맞는 지적이었다. v44~v48 은 사다리를 타고 **배 위로 몸이 나가** 포탑에
//    섰다. 그러면 진공에 맨몸으로 서 있는 셈이고, 무엇보다 「정비공이 배
//    안에서 일한다」는 이 게임의 전제와 어긋난다.
//
//  ★ 찾아본 것 둘 — **실제로 그렇게 만든다**
//    ① **B-29 중앙 사격 통제**(Sperry). 포탑은 기체 밖에 있고 사수는
//       **여압된 안쪽에 앉아** 관측창(astrodome)의 조준경으로 겨눈다.
//       포탑은 **원격**으로 돈다. 사장님이 말씀하신 그림 그대로다
//    ② **밀레니엄 팰컨 건너석**. 좌석에 **손잡이 둘**이 붙어 있고 사수는
//       좌석에 앉아 몸째 돈다. 손잡이를 잡는 손짓이 여기서 왔다
//
//    그래서 이 배의 주포는 **원격 포탑 + 실내 조준석**이다.
//    사다리로 올라가는 것이 아니라 **좌석에 앉는다.**
// ══════════════════════════════════════════════════════════════════════════
export const GUN = {
  /**
   * ★ **앉고 일어나는 데** (초). 예전에는 사다리를 2.2초 올랐다 —
   *   밖으로 나가지 않으니 그만큼 오래 걸릴 이유가 없다.
   *   다만 0 은 아니다. **자리를 옮기는 것**이라야 「동시에 두 곳에 못
   *   있는다」가 성립한다.
   *
   *   ★ 1.1 로 잡았다가 도구가 「왕복 2.2초 — 잠깐 올라갔다 오기가 된다」로
   *     빨개졌다. 사다리(2.2)보다 빨라야 한다는 것과 **자리를 옮기는 값이
   *     있어야 한다**는 것은 둘 다 맞다 — 사이를 잡는다 (왕복 3.6초)
   */
  climb: 1.8,
  /**
   * ★ **WASD 로 포탑을 돌린다** (도/초). 사장님 「wasd로 조준하는 것으로」.
   *   너무 빠르면 겨누는 것이 아니라 휘두르는 것이 되고, 너무 느리면
   *   지나가는 것을 못 따라간다 — 떠도는 것들이 초당 3.5도까지 흐르므로
   *   그보다 넉넉히 빨라야 따라잡는다
   */
  aimRate: 26,
  /**
   * 포탑에서 **한 발 쏘는 데** 걸리는 시간 (초).
   * ★ 연사가 아니다. 한 발마다 「쏠까」가 다시 생겨야 선택이 유지된다
   */
  reload: 3.4,
  /**
   * 조준을 얼마나 정확히 해야 맞나 (0~1 · 1 이면 정면).
   * ★ 추격자에게 쏠 때만 쓴다 — 떠도는 것들은 **도(度)로** 잰다
   *   (`target-table.js aimTol`). 보이는 것과 안 보이는 것은 자가 다르다
   */
  aimNeed: 0.72,

  /**
   * 한 발에 드는 것.
   * ★ **광석이 먼저, 없으면 부품**이다. 광석은 팔아서 식량·부품이 되는
   *   것이라 「쏘면 다음 거점에서 못 산다」가 되고, 부품까지 쓰기 시작하면
   *   「쏘면 다음 고장을 못 고친다」가 된다 — 벌이 두 단계로 깊어진다
   */
  costOre: 6,
  costParts: 1,

  /** 맞으면 추격자가 이만큼 뒤로 (거리 0~100) */
  hitDist: 17,
  /** 빗나가도 **조금은** 벌어진다 — 피하느라 (0 이면 「빗나감」이 그냥 손해다) */
  missDist: 4,

  /**
   * ★ **쏘면 자국이 이만큼 오른다** (열 단위로 환산해서 얹는다).
   *   총구 섬광은 숨을 수 없다 — 이게 「쏘고 도망」을 막는 유일한 자리다
   */
  flashSign: 26,
  /** 그 밝기가 이 초 동안 남는다 */
  flashFor: 22,

  /** 포탑에 있는 동안 열이 초당 이만큼 더 오른다 — 포도 뜨겁다 */
  heatPerShot: 5,

  /**
   * ★ 포탑에 매인 시간이 회차의 이 비율을 넘으면 **장르가 바뀐 것**이다.
   *   `FLYING.md` 가 조종석에 걸어 둔 안전핀과 같은 규약 —
   *   넘으면 정비공이 아니라 포수다. 도구가 이걸 본다
   */
  seatShareMax: 0.12,
};

/** 쏠 수 있나 — 무엇으로 쏘나 */
export function ammoFor(supply) {
  if ((supply?.ore ?? 0) >= GUN.costOre) return 'ore';
  if ((supply?.parts ?? 0) >= GUN.costParts) return 'parts';
  return null;
}

/** 왜 못 쏘나 — **조용히 안 나가면 「고장났다」로 읽힌다** */
export const WHY = {
  ammo: '실을 것이 없습니다 — 광석이나 부품이 있어야 합니다',
  reload: '재는 중입니다',
  aim: '겨눠야 합니다',
  calm: '쏠 상대가 없습니다',
};

export function whyNotFire(s = {}) {
  // ★★ **`if (!s.chasing) return 'calm'` 이 있었다 — 그게 주포를 못 쓰게 했다.**
  //   사장님: 「주포도 조작이 안되고, 적이 있던 없던 가능하도록해」
  //
  //   맞는 지적이다. 쫓길 때만 쏠 수 있으면 **연습을 못 한다** — 처음
  //   쏘는 순간이 하필 제일 급한 순간이고, 그때 「겨눠야 합니다」를 처음
  //   배운다. 그건 가르침이 아니라 시험이다.
  //
  //   막지 않아도 되는 이유는 **쏘는 것이 이미 공짜가 아니어서**다:
  //   탄약이 곧 수리 재료고(`costOre`·`costParts`), 쏘면 밝아지고
  //   (`flashFor`), 재는 데 시간이 든다. 허공에 쏘면 그 값을 그냥 버린다 —
  //   막을 것이 아니라 **치르는 것**이다 (이 게임의 벌은 늘 그랬다)
  if (!ammoFor(s.supply)) return 'ammo';
  if ((s.cool ?? 0) > 0) return 'reload';
  if ((s.aim ?? 0) < GUN.aimNeed) return 'aim';
  return null;
}
