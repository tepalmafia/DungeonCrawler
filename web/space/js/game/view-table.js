// ══════════════════════════════════════════════════════════════════════════
//  ★★★ 조종석 유리와 콘솔의 **자리** — 시야를 정하는 숫자 (v65)
//
//  ★ 왜 `world/cockpit.js` 에서 여기로 옮겼나
//    시야각은 이 좌표들에서 나오는 값이다 (`atan(크기 / 거리)`). 그런데
//    `cockpit.js` 는 three.js 를 쓰므로 **`tools/` 가 못 읽는다** — 그래서
//    「시야가 몇 도인가」를 재려면 브라우저를 띄워야 했고, 이 환경에서
//    그건 한 번에 몇 분이다. **잴 수 있는 것은 표에서 잰다** (CLAUDE.md).
//
//  ★ three.js 를 안 쓴다 — tools/space-view.js 가 브라우저 없이 읽는다.
//
//  ══════════════════════════════════════════════════════════════════════
//  ★★ **v65 에서 통째로 앞으로 당겼다** (사장님 「시야가 작다」)
//
//    잰 값 — v64 까지 조종간을 잡았을 때:
//      세로 창 **34.6도** · 화각 94 · 창이 화면의 **37%**
//
//    창의 세로 폭이 35도인데 화각이 94도였다. **아무리 잘해도 창은
//    화면의 37% 를 못 넘는다** — v63 이 화각을 넓힌 것은 맞았지만
//    넓힌 화각을 채울 유리가 없었다.
//
//    각도는 `atan(크기 / 거리)` 다. **유리를 두 배 키우는 것과 눈을
//    절반 붙이는 것이 같다.** F-16 이 「버블」인 이유는 유리가 큰 게
//    아니라 **머리 바로 옆**이기 때문이다 (세로 195도 · 기수 너머 15도).
//    그래서 좌석·콘솔·유리를 **한 덩어리로** 앞으로 밀었다.
//
//  ★ 견주는 값 (찾아본 것)
//      FAA AC 25.773-1   기수 너머 아래 **17도**
//      F-16              수평 360 · 세로 195 · 기수 너머 **15도**
//      F-35              수평 340 · 세로 188.5 · 기수 너머 **16도**
// ══════════════════════════════════════════════════════════════════════════

/**
 * 캐노피가 꺾이는 여섯 점 (x, z). **평면 하나가 아니라 꺾인 띠**다 —
 * 이것 하나로 「감싸는 조종석」이 되는지가 갈린다.
 *
 * ★★ **x = 0 근처에 점이 없다.** 점마다 기둥(멀리언)이 서므로,
 *   가운데에 점을 두면 **정면 한복판에 기둥**이 선다. 실제 전투기는
 *   12시에 아무것도 안 둔다 (F-16 이 「틀 없는」 버블인 이유).
 *   `space-view.js [3]` 이 이걸 지킨다
 */
export const CANOPY = [
  [-3.00, -7.55],
  [-2.55, -8.95],
  [-1.05, -9.30],
  [1.05, -9.30],
  [2.55, -8.95],
  [3.00, -7.55],
];

/**
 * 콘솔이 그리는 호. **충돌도 이 값을 읽는다** — 두 곳에 적으면 갈라진다.
 *
 * ★ 좌석과 **같이** 움직인다. 따로 움직이면 v61 에서 맞춰 둔 DEP 관계
 *   (눈 → 조종간 → 계기)가 통째로 깨진다
 */
export const CONSOLE_PTS = [
  [-2.30, -8.30], [-1.30, -8.97], [-0.45, -9.23],
  [0.45, -9.23], [1.30, -8.97], [2.30, -8.30],
];

/**
 * 유리의 위아래 끝 (m).
 * ★ v65 에서 늘렸다 — 아래끝 0.82 → 0.58 · 위끝 2.34 → 2.62.
 *   가까워진 유리에 폭까지 더해야 「감싼다」가 된다
 */
export const GLASS_Y = { sill: 0.58, head: 2.62 };

/**
 * ★★ **눈썹 차양(glareshield)의 윗면** — 여기 위로 밖을 본다.
 *
 *   앉은 눈(1.20)보다 **아래**여야 한다. 실제 조종석의 차양은 설계 눈점
 *   보다 5~10cm 아래에 있고, 조종사는 그 위로 지면을 본다.
 *   v61 까지 이 값이 **1.47** 이었다 — 앉은 눈보다 27cm **위**였고,
 *   그래서 앉으면 눈앞이 계기였다. DEP 로 짓는다는 것은 눈높이만 정하는
 *   것이 아니라 **눈 아래로 계기를 내리는 것**까지다
 */
export const BROW_Y = 1.02;

/** 대시 얼굴(화면이 박히는 판)의 가운데 높이 */
export const DASH_Y = 0.90;
/** 스위치 선반 — 무릎 가림판 위, 대시 아래 */
export const SHELF_Y = { lo: 0.52, hi: 0.76 };
