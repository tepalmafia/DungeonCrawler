// ══════════════════════════════════════════════════════════════════════════
//  하늘 — **고증한 숫자만 여기 있다** (2026-08-07)
//
//  ★ 사장님: 「우주 배경을 실사화해줘. 인터넷에서 고증해서. 행성도 그렇고
//             지금은 하얀색이 너무 많이 날아오는데?」
//
//  ★★ 「하얀색이 너무 많이 날아온다」는 **두 가지가 겹친 것**이었다.
//     둘 다 화면을 찍어 보고서야 갈렸다 (docs/POSTMORTEM.md §3):
//
//     ① **네모였다.** three 의 기본 점(PointsMaterial)은 지도(map)를 안 주면
//        **정사각형**을 그린다. 900 개가 전부 흰 사각형이라 별이 아니라
//        눈보라였다. 이건 「너무 많다」가 아니라 **모양이 별이 아니었다**
//     ② **흘렀다.** 가까운 별 900 개를 초속 몇 유닛으로 **뒤로 흘려보내고**
//        있었다. 이건 고증으로 틀렸다 — 제일 가까운 별도 4광년이라
//        어떤 배로도 별은 **한 번도 안 움직인다**. 흐르는 별은
//        스타워즈에서 온 그림이지 우주에서 온 것이 아니다
//        (space.com — 학생들이 물리로 뒤집은 그 계산)
//
//     그래서 고친 것은 개수가 아니라 **무엇이 흐르는가**다.
//     별은 **박아 두고**, 흐르는 것은 **먼지**로 바꾼다. 배가 움직인다는
//     느낌은 먼지 130 개면 난다 — 별 900 개는 그 일을 하고 있지도 않았다.
//
//  ★ 고증한 것 (2026-08-07 · 인터넷)
//    · 우주에서 별은 **깜빡이지 않는다.** 대기가 없어서 흔들 것이 없다.
//      정지한 **점**이다 (NASA · ISS 촬영 기록)
//    · 은하수는 **띠**로 보이고, 대기가 없어 지상보다 밝다 —
//      「빛나는 리본」·「먼지 필라멘트」 (ISS 사진 설명 그대로)
//    · 밝기는 **아주 치우쳐** 있다. 1등성보다 밝은 별은 온 하늘에 22 개,
//      6등성은 5,950 개다 — 한 등급마다 **세 배씩** 늘어난다
//    · 궤도에서 본 행성에는 **터미네이터**(낮/밤 경계)가 있고, 그 선은
//      **또렷하지 않다** — 대기가 빛을 흩어서 황혼이 번진다
//    · 대기는 **아주 얇은 파란 테두리**다. 우주비행사 Ed Lu 의 말로
//      「지평선에서 1도 남짓 — 팔 뻗은 손가락 굵기」
//
//  ★ 어디서 봤나 (2026-08-07)
//    · 흐르는 별이 물리로 틀렸다 — space.com/19268-star-wars-hyperspace-physics-reality.html
//    · 우주에서는 별이 안 깜빡인다 — space.com/why-do-stars-twinkle
//    · 은하수는 「빛나는 리본 · 먼지 필라멘트」 — nasa.gov 「Glowing Views from
//      the Space Station」 · earthobservatory.nasa.gov 「Stargazing from the ISS」
//    · 등급별 개수 — skyatnightmagazine.com · astronomytrek.com (6등급까지 약 8,800)
//    · 분광형과 색 — britannica.com 「Stellar colours」 (O 파랑 ~ M 붉은주황)
//    · 대기가 얇다 — 우주비행사 Ed Lu 「지평선에서 1도 남짓」
//    · 터미네이터가 번진다 — nasa.gov 「Thin Blue Line」 · earthobservatory 황혼 사진
//
//    ★★ **사진에서 밝기를 가져오면 안 된다.** 은하수를 사진 밝기로 넣었다가
//      창 전체가 회색 안개가 됐다 — 사진은 셔터를 몇 초 열어 모은 것이고
//      눈은 그렇지 않다. 고증에서 **모양은 가져오되 세기는 눈 기준으로** 잡는다.
//
//  ★ three.js 를 안 쓴다 — `tools/space-sky.js` 가 브라우저 없이 읽는다
// ══════════════════════════════════════════════════════════════════════════

/**
 * ★★ **등급 띠** — 온 하늘에 몇 개인가. 이 표가 「실사」의 8할이다.
 *
 * 전에는 별 3,100 개가 **전부 같은 크기·같은 흰색**이었다. 그러면 아무리
 * 예쁘게 그려도 별하늘이 안 된다 — 진짜 하늘은 **밝은 것 몇 개와 희미한 것
 * 수천 개**이고, 그 대비가 곧 깊이다.
 *
 * 숫자는 실제 성표(6등급까지 약 8,800)를 그대로 쓴다. 점 8,800 개는
 * **그리기 한 번**이라 2,200 개보다 안 비싸다 — 비싼 것은 개수가 아니라
 * 드로우콜이다 (SPACE.cost 로 재 봤다).
 *
 * @property to   이 띠의 어두운 쪽 등급
 * @property n    온 하늘의 개수 (실제 성표)
 * @property size 화면에 몇 픽셀로 그리나. **밝을수록 크다** — 실제로 별이
 *                커서가 아니라 밝으면 눈과 렌즈에서 번지기 때문이고,
 *                그래서 이 값이 곧 「밝아 보임」이다
 * @property lum  색의 세기 (0~1)
 * @property warm 색이 얼마나 살아 있나. **어두운 별은 색을 잃는다** —
 *                사람 눈의 원추세포가 약한 빛에서 안 켜져서, 실제로도
 *                6등성은 무슨 색이든 다 회백색으로 보인다. 0 이면 흰색
 */
export const MAGS = [
  { to: 1.0, n: 22, size: 7.4, lum: 1.00, warm: 1.00 },
  { to: 2.0, n: 71, size: 5.0, lum: 0.80, warm: 0.90 },
  { to: 3.0, n: 190, size: 3.5, lum: 0.60, warm: 0.72 },
  { to: 4.0, n: 610, size: 2.5, lum: 0.42, warm: 0.50 },
  { to: 5.0, n: 1930, size: 1.8, lum: 0.28, warm: 0.28 },
  { to: 6.0, n: 5950, size: 1.35, lum: 0.17, warm: 0.12 },
];

/** 별은 다 해서 몇 개인가 — 표에서 **센다.** 손으로 적으면 갈라진다 */
export const STAR_COUNT = MAGS.reduce((a, m) => a + m.n, 0);

/**
 * ★ **분광형** — 별에 색이 있다.
 *
 * ★ 여기서 한 번 틀릴 뻔했다. 「우주에 제일 많은 별은 M 형(적색왜성)이고
 *   그게 76%」라는 것이 검색 첫 줄에 나오는데, **그건 눈에 보이는 별의
 *   분포가 아니다.** 적색왜성은 어두워서 맨눈으로는 하나도 안 보인다.
 *   눈에 보이는 별(6등급까지)은 오히려 **뜨거운 쪽으로 치우쳐** 있다 —
 *   멀어도 보일 만큼 밝은 별만 목록에 들어오기 때문이다.
 *   **재는 대상을 틀리면 고증이 아니라 그냥 틀린 값이다.**
 *
 * @property w    맨눈에 보이는 별 중 몇 %인가 (밝은별 성표 기준)
 * @property rgb  그 온도의 흑체가 내는 색 (Mitchell Charity 표 근사)
 */
export const SPECTRA = [
  { cls: 'O', k: 40000, w: 0.004, rgb: [0.61, 0.69, 1.00] },
  { cls: 'B', k: 20000, w: 0.190, rgb: [0.67, 0.75, 1.00] },
  { cls: 'A', k: 9000, w: 0.240, rgb: [0.83, 0.88, 1.00] },
  { cls: 'F', k: 7000, w: 0.140, rgb: [0.97, 0.97, 1.00] },
  { cls: 'G', k: 5500, w: 0.100, rgb: [1.00, 0.96, 0.86] },
  { cls: 'K', k: 4500, w: 0.240, rgb: [1.00, 0.86, 0.68] },
  { cls: 'M', k: 3200, w: 0.086, rgb: [1.00, 0.72, 0.52] },
];

/**
 * ★ **은하수** — 화면에서 제일 크게 달라지는 것.
 *
 * 지금까지 창밖은 **어디를 봐도 똑같았다.** 별을 고르게 뿌렸기 때문인데,
 * 진짜 하늘은 그렇지 않다 — 은하 원반 안에 있으므로 **한쪽에 띠**가 있다.
 * 띠가 생기면 「어느 쪽을 보고 있나」가 생기고, 그게 곧 방향 감각이다.
 *
 * @property tilt  은하면이 배의 수평면에서 몇 라디안 기울었나. **0 이면 안 된다** —
 *                 배와 나란한 띠는 우연이 아니라 실수로 보인다
 * @property yaw   좌우로 얼마나 돌렸나
 * @property crowd 별의 몇 할을 띠 쪽으로 몰아 주나 (0 = 고르게)
 * @property width 띠의 두께 (은하 위도, 라디안). 실제 은하수는 ±10도쯤이
 *                 밝고 그 바깥으로 흐려진다
 * @property glow  띠 자체의 흐린 빛 세기. 별 하나하나로는 안 되는 부분이다 —
 *                 은하수의 밝기 절반은 **낱개로 안 갈라지는 별**이다
 * @property dust  띠를 가르는 어두운 먼지 줄기의 세기 (석탄자루·큰틈)
 */
export const MILKYWAY = {
  tilt: 0.42, yaw: 1.15,
  crowd: 0.55, width: 0.16,
  // ★★ **0.30 으로 만들었다가 화면을 찍고 내렸다.** 창 전체가 **회색 안개**로
  //   덮였다 — 은하수가 아니라 「유리가 더럽다」로 보였다. 은하수의 실제
  //   표면 밝기는 아주 낮다(맨눈으로 어두운 데서나 겨우 보이는 정도)는데
  //   나는 사진에서 본 밝기를 넣고 있었다. **사진은 몇 초를 모은 것**이고
  //   눈은 그렇지 않다 — 고증을 사진에서 가져올 때 늘 밟는 함정이다
  glow: 0.085, dust: 0.55,
};

/**
 * ★★ **먼지** — 흐르는 것은 이제 이것뿐이다.
 *
 * 별을 흘리는 대신 먼지를 흘린다. **개수를 900 에서 130 으로 줄였는데
 * 「움직인다」는 그대로다** — 움직임을 만드는 것은 개수가 아니라
 * 시차(가까운 것이 빨리 지나감)이기 때문이다. 900 개는 그저 눈보라였다.
 *
 * @property n     몇 알
 * @property size  화면 픽셀. 별의 제일 어두운 띠(1.35)보다 **작게** 둔다 —
 *                 커지는 순간 다시 「하얀 게 날아온다」가 된다
 * @property lum   밝기. 별빛만 받는 티끌이라 아주 어둡다
 * @property rgb   **흰색이 아니다.** 규산염 티끌은 회갈색이다
 * @property near  이보다 뒤로 오면 되돌린다 (창 바로 앞)
 * @property far   되돌아가는 자리
 * @property fadeIn  나타날 때 몇 유닛에 걸쳐 켜지나 — **툭 생기면 눈에 띈다**
 * @property fadeOut 사라질 때. 창 앞에서 갑자기 꺼지면 그게 제일 어색하다
 */
/**
 * ══ ★★★ **빛줄기** — 속도감은 점이 아니라 **선**에서 나온다 (v87) ═══════
 *
 *  ★ 사장님 「**정지한 상태인지 이동중인지 속도감이 안느껴지잔아** …
 *    R 누르면 더 속도감이 느껴져야하고, **빛무리**? 우주에서 속도감을
 *    느낄수 잇는 부분들을 **고증**해서 적용해」
 *
 *  ══ 재 보니 숫자는 이미 컸다 ════════════════════════════════════════
 *
 *    추력 켬/끔 = 2.2배 · 급가속 = 거기서 또 **7.5배**.
 *    그런데 **알갱이가 점**이라 아무리 빨리 흘러도 「빨라졌다」로 안
 *    읽힌다 — 점은 자리만 바뀌지 **모양이 안 바뀐다.**
 *
 *  ══ ★★★ 고증 — 우주에서 속도감이 나는 곳은 셋뿐이다 ═══════════════
 *
 *    ① **가까운 것의 시차** — 먼 별은 안 흐른다 (4광년). 코앞의 티끌만
 *       흐른다. v57 에 못박아 둔 그대로다
 *    ② **가속** — 속도가 아니라 **가속**이 몸에 느껴진다. 등속으로 가는
 *       동안은 아무 느낌이 없다 (실제 우주비행이 그렇다)
 *    ③ **노출 시간 동안의 자국** — 빠르게 지나간 것은 눈·카메라에
 *       **선으로** 남는다. 「빛무리」의 정체가 이것이다
 *
 *  ★★ 그래서 ③ 을 만든다: 티끌이 **점에서 줄로** 바뀐다. 길이는 얼마나
 *    빨리 지나가나에 비례한다 — 없는 것을 그리는 것이 아니라 **원래
 *    그렇게 보이는 것**을 그리는 것이다.
 *
 *  ══ ★★★ 처음엔 **급가속에만** 켰다가 고쳤다 (v87 → v88) ═══════════════
 *
 *    사장님이 물으신 것은 **「정지한 상태인지 이동중인지」**였는데,
 *    첫 판은 `boost.k` 로만 줄을 켰다. 즉 **R 을 안 누르면 v73 이전과
 *    똑같았다** — 물어보신 그 상태 그대로다. 화면을 찍고서야 봤다:
 *    급가속 화면은 확 달라졌는데 **등속 화면은 한 톨도 안 바뀌었다.**
 *
 *    ★ 그래서 줄은 **실제로 흐르는 속도**가 켠다 (`boost` 가 아니라).
 *      급가속은 그 속도를 올릴 뿐이므로 저절로 따라온다 — 축이 하나다.
 *
 *  ══ ★★ 일부러 접은 것 — **선형이 아니다** ══════════════════════════════
 *
 *    노출 자국은 **속도에 정비례**한다. 그런데 이 게임의 속도 폭이
 *    정박 6.5 ~ 급가속 663 으로 **100배**라, 정비례로 그리면 급가속을
 *    빼고는 전부 **한 화소 밑**이 된다 — 있으나 마나다.
 *    그래서 `bend`(0.32) 로 **눌러 편다**: 느린 쪽이 벌어지고 빠른 쪽이
 *    좁혀진다. 고증을 어긴 자리이고, **어긴 줄 알고 어긴다.**
 *
 * @property len  제일 빠를 때(급가속 한창)의 줄 길이 (m)
 * @property fast 그 「제일 빠를 때」가 초당 몇 유닛인가
 * @property min  이 아래로는 아예 안 그린다. **정박(6.5)이 여기 걸린다** —
 *                「대고 있으면 줄이 없다」가 곧 정지의 신호다
 * @property bend 눌러 펴는 정도. 1 이면 정비례(=거의 안 보임) · 작을수록 편다
 * @property lum  밝기
 */
export const STREAK = { len: 30, fast: 663, min: 8, bend: 0.32, lum: 0.55 };

/**
 * ★★★ 지금 흐르는 속도로 줄 길이를 낸다 — **게임과 도구가 같은 식을 쓴다.**
 * @param mps 먼지가 초당 몇 유닛 흐르나
 * @returns 줄 길이 (0 이면 안 그린다)
 */
export function streakLen(mps) {
  if (!(mps > STREAK.min)) return 0;
  const k = (mps - STREAK.min) / (STREAK.fast - STREAK.min);
  return +(STREAK.len * Math.min(1, Math.max(0, k) ** STREAK.bend)).toFixed(2);
}

export const DUST = {
  n: 130, size: 1.1, lum: 0.36, rgb: [0.78, 0.75, 0.70],
  spread: 46, near: 0.5, far: 150, fadeIn: 40, fadeOut: 26,
  /**
   * ★★★ **v78 — 먼지가 배를 둘러싼다** (사장님 요청).
   *
   *   「지금 **처음 시작 화면만** 가속도가 느껴지는 물체가 날아오는게
   *   보이는데 **방향을 틀면 안보여서 r 을 눌러도 효과가 안나와.**
   *   … **어떤 우주를 바라보든지** 우주먼지나 빛이 보이도록」
   *
   *   ★ 맞는 지적이었다. 먼지는 **창 앞에 깔린 원반 한 장**이었다
   *     (반지름 46 · 앞으로 150). 기수를 돌리면 그 판에서 눈이 벗어나고,
   *     그러면 급가속(R)의 속도감이 **통째로 사라진다** — v73 에 속도감을
   *     넷이나 만들어 놓고 **정면을 볼 때만** 살아 있었던 셈이다.
   *
   *   ★★ 그래서 **부피**로 바꾼다: 배를 가운데 두고 한 변 `box` 의 상자를
   *     채우고, 뒤로 지나간 알갱이는 앞으로 되돌린다. 어느 쪽을 보든
   *     알갱이가 있고, 흐르는 방향은 **가는 방향** 그대로다.
   *
   *   ★ 고증도 이쪽이 맞다. 성간 티끌은 배 주위에 **고르게** 있지 앞에만
   *     깔려 있지 않다. 앞만 보이던 것이 이상했던 것이다.
   *
   * @property box   상자 반 변 (m). 이 안을 알갱이가 채운다
   * @property fadeR 이보다 가까우면 흐려진다 — 코앞의 알갱이는 선이 된다
   */
  box: 55,
  fadeR: 6,
};

/**
 * ★★ **행성** — 터미네이터와 대기 테두리.
 *
 * 전에는 `MeshBasicMaterial` 짜리 **민짜 공**이었다. 조명을 안 받으니
 * 어느 쪽이 낮인지가 없고, 그래서 「어두운 파란 원반」으로 보였다 —
 * 화면을 찍어 보면 행성인지 창에 난 얼룩인지 구별이 안 된다.
 *
 * ★ 그런데 **조명(Light)을 놓으면 안 된다.** 예전에 태양을 하나 놓았다가
 *   `DirectionalLight` 에 거리가 없어서 **배 안까지 같이 밝아졌다** —
 *   창밖을 고치려다 실내 조명을 통째로 망가뜨리는 종류의 사고다.
 *   그래서 빛은 **행성 자기 셰이더 안에서만** 계산한다. 장면의 조명 목록에
 *   아무것도 안 보태므로 실내로 샐 길이 없다.
 *
 * @property sun       태양이 어느 쪽에 있나 (단위벡터). 옆에서 비춰야
 *                     터미네이터가 원반 한가운데를 지난다 — 정면이면
 *                     보름달이라 경계가 아예 안 보인다
 * @property soft      터미네이터가 얼마나 번지나. **0 이면 안 된다** —
 *                     대기가 빛을 흩어서 실제로는 또렷한 선이 아니다
 * @property twilight  경계에 뜨는 붉은 띠 (황혼). ISS 사진의 그 색
 * @property night     밤면 밝기. **0 이 아니다** — 완전히 까맣게 두면
 *                     행성이 반달로 잘려 보이고, 실제로는 별빛에 윤곽이 남는다
 * @property limb      대기 테두리의 세기
 * @property limbPow   테두리가 얼마나 얇나. 클수록 얇다 —
 *                     **얇아야 고증이다** (Ed Lu 「1도 남짓」)
 * @property air       대기 껍질이 지표보다 몇 배 큰가. 1.03 은 지구로 치면
 *                     190km 라 실제(약 100km)보다 두껍지만, 이보다 얇게
 *                     주면 화면에서 한 픽셀도 안 나온다
 */
export const PLANET = {
  sun: [-0.72, 0.36, 0.59],
  soft: 0.22, twilight: [1.00, 0.46, 0.22], night: 0.055,
  // ★★ 황혼 띠의 **폭**. `soft` 를 그대로 쓰다가 화면을 찍고 갈랐다 —
  //   원반의 3분의 1 이 주황으로 물들어서 행성이 **분홍 반죽**으로 보였다.
  //   경계가 번지는 폭(soft)과 황혼이 보이는 폭은 **다른 값**이다.
  //   황혼은 지구로 치면 수백 km 라 원반에서는 실오라기 한 줄이다
  // ★ 세기도 내렸다. 0.55 로 찍으니 **분홍 리본**이 원반을 가로질렀다 —
  //   황혼은 눈에 띄어야 하지만 행성보다 세면 그건 띠가 아니라 장식이다
  twiWidth: 0.075, twiStrength: 0.20,
  // ★★ **limb 0.85 · limbPow 3.4 로 만들었다가 화면을 찍고 고쳤다.**
  //   테두리가 아니라 **후광**이었다 — 행성 둘레로 넓게 퍼져서, 고증한
  //   「팔 뻗은 손가락 굵기」와 정반대였다. 세기를 3분의 1로 내리고
  //   지수를 올려 **가장자리에만** 서게 한다. 지수가 클수록 얇다
  //   ★ 그런데 0.30 · 지수 6.0 은 이번엔 **아예 안 보였다.** 두 번 헛짚고
  //     나서야 자리를 잡았다 — 지수가 너무 크면 테두리가 한두 픽셀이라
  //     없는 것과 같다. 「얇지만 분명히 보인다」가 고증이다
  limb: 0.60, limbPow: 4.5, air: 1.028,
  /** 구역용 행성 — 차갑고 먼 가스행성 쪽으로 잡았다 */
  day: [0.25, 0.35, 0.55], dark: [0.04, 0.06, 0.11], sky: [0.42, 0.66, 1.00],
  /** 내려가는 행성 — 마른 땅. 착륙 때만 이 색으로 갈아탄다 */
  landDay: [0.80, 0.60, 0.40], landDark: [0.16, 0.10, 0.07], landSky: [1.00, 0.76, 0.52],
  /** 지표 무늬의 세기. 민짜 공은 공으로 보이지 행성으로 안 보인다 */
  mottle: 0.16,
};

/** 별 천구의 반지름. **카메라 far(400) 보다 넉넉히 안쪽**이라야 안 잘린다 */
export const DOME_R = 330;

/**
 * ★ 밝은 별 하나 — **태양이다.**
 *
 * 행성에 낮과 밤이 있는데 **비추는 것이 하늘에 없으면** 그건 「왜 저쪽만
 * 밝지」가 된다. 아주 밝은 점 하나를 `PLANET.sun` 방향에 둔다.
 * 원반으로 안 그린다 — 이 배는 항성계 하나를 지나가는 중이 아니라
 * 성간을 가고 있고, 그 거리에서 태양은 **아주 밝은 별**이다.
 */
export const SUN = { size: 26, lum: 1.0, rgb: [1.0, 0.97, 0.90] };
