// ══════════════════════════════════════════════════════════════════════════
//  장면 — **긴장에 모양을 준다** (docs/space/PLAN2H.md §1~§6)
//
//  ★ 앞 판에서 내가 틀린 것
//    「겹침을 1 → 2 → 3 으로 늘린다」고 적었다. 그건 **음량 조절이지
//    설계가 아니다.** 시끄러움을 키우는 것과 무서워지는 것은 다르다.
//    사장님이 그 자리를 정확히 짚으셨다 — 「계속 일들이 겹치면 안 되고」.
//
//  ★ 그래서 뒤집었다
//    사건이 **무작위로 오는 것**을 **배치로** 바꾼다. 구간마다 **이름이
//    있는 장면 하나**가 오고, 겹치는 것은 2시간에 **딱 두 번**이다.
//    그래서 그 두 번이 무섭다.
//
//  ★ 여덟이 **다 다른 방에 사람을 묶는다**
//    그게 「같은 것 또 하네」를 막는 유일한 방법이다. 세기를 올리는 것으로는
//    안 된다 — 같은 방에서 같은 손짓을 세게 하면 그냥 더 지겹다.
//
//  ★ **네 박자를 표가 갖는다** (§2)
//    예고 → 대응 → 해소 → 여운. 특히 **여운을 표에 적어 둔다** —
//    「여기가 시간 가는 줄 모른다의 자리」인데, 코드에 흩어 두면 장면을
//    하나 보탤 때마다 조용히 빠진다.
//
//  ★ three.js 를 안 쓴다 — tools/space-2h.js 가 브라우저 없이 읽는다.
// ══════════════════════════════════════════════════════════════════════════

/**
 * 네 박자의 길이(초). **장면마다 다르지 않다** — 여기 한 곳에 둔다.
 *
 * ★ 장면별로 따로 주고 싶어지는데 참는다. 여덟 개가 저마다 다른 박자를
 *   가지면 「무엇이 오는지 몰라도 리듬은 안다」가 무너진다. 다르게 하는
 *   것은 **묶이는 방과 손이 하는 일**이지 박자가 아니다.
 */
export const BEAT = {
  /** 「온다」 — 준비할 시간이 있어야 **선택**이 된다. 없으면 사고다 */
  warn: [20, 40],
  /** 「어떻게든」 — 여기가 긴장이다 */
  act: [120, 240],
  /** 「됐다」 — 이미 만든 「뿌리친 3초」가 여기 붙는다 */
  clear: [3, 10],
  /**
   * 「정리한다」 — ★ **여기가 「시간 가는 줄 모른다」의 자리다.**
   * 여운이 없으면 지치고, 여운이 길면 시계를 본다.
   */
  after: [40, 90],
};

/**
 * 장면 여덟. **`room` 이 서로 달라야 한다** — 그게 이 표의 검사 조건이다.
 *
 * | 칸 | 뜻 |
 * |---|---|
 * | `key` | 배치표가 부르는 이름 |
 * | `name` | 사람에게 보이는 이름 (끝 화면 목록에 그대로 쓴다) |
 * | `lead` | 예고 박자에 뜨는 한 줄. **어디가 잘못됐나는 안 가르친다** |
 * | `room` | **사람이 묶이는 방.** 여덟이 다 달라야 한다 |
 * | `hands` | 손이 하는 일 (한 줄) |
 * | `uses` | 어느 계통을 켜나 — 이미 있는 것에 얹는지 새로 만드는지가 여기서 보인다 |
 * | `built` | **지금 게임에 있나.** 없으면 배치는 해 두되 안 뜬다 |
 */
export const SCENES = {
  A: {
    key: 'A', name: '적이 붙는다',
    lead: '자국이 굵어집니다',
    // ★★★ v112 — **손이 바뀌었다.** 「열을 내린다 · 전력을 고른다」는
    //   기관실에 걸어가서 하던 일이고, 그 방이 없어졌다 (v109).
    //   이제 같은 값을 **조종석에서** 치른다: 뿌리치거나 부순다
    from: 'foe', hands: '뿌리치거나 부순다 — 쏘면 뜨거워지고 뜨거우면 못 숨는다',
    uses: ['chase', 'heat', 'might'],
    /**
     * ★ **대응 박자부터** 붙는다. 예고 중에는 자국만 굵어지고 아직 안 온다.
     *   「온다」와 「왔다」가 같은 순간이면 그건 예고가 아니라 **사고**다
     *   (§2 — 준비할 시간이 있어야 선택이 된다).
     */
    opensAt: 'act',
    built: true,          // game/chase.js — 이미 돈다
  },
  B: {
    key: 'B', name: '행성에 내린다',
    lead: '전방에 중력원 — 내릴 자리를 확인하십시오',
    // ★ v112 — 관측실에서 고르던 것이 **조종석 갈래 판**으로 왔다 (v66).
    //   에어록에서 싣던 것은 **화물칸**이 받는다
    from: 'place', hands: '내릴지 고르고 · 진입각을 잡고 · 싣는다',
    uses: ['route', 'yoke', 'supply', 'cargo'],
    /**
     * ★ **이름과 손이 바뀌었다** (2026-08-06 · 사장님 「행성에 착륙해서
     *   가지고 올 수 있도록」). 원래는 「행성을 만난다 · 스윙바이할지 돌지
     *   고른다」였는데, 그건 **해도대에서 한 번 누르는 것**이라 장면이
     *   될 만큼이 안 된다. 사장님이 말씀하신 「일련의 과정」이 맞다.
     *
     * ★ **여덟 중 이것만 방 셋을 거친다.** 나머지는 한 방에 묶는데
     *   B 는 끌고 다닌다 — 관측실(결심) → 조종석(진입) → 에어록(싣기).
     *   `room` 은 장면이 **열리는** 방이라 관측실 그대로다
     */
    built: true,          // game/land.js — v46 부터 돈다
  },
  C: {
    key: 'C', name: '자동 조종이 죽는다',
    lead: '밀집 구역 — 자세 제어를 확인하십시오',
    from: 'ship', hands: '손으로 편다 — 놓으면 배가 돈다. 보조도 못 따라잡는다',
    uses: ['hazard', 'yoke', 'assist'],
    /** 자세 제어는 **지대 한복판에서** 나간다 (§4-2) — 예고 때는 멀쩡하다 */
    opensAt: 'act',
    built: true,          // game/drift.js — v39 부터 돈다
    /**
     * ★ **이 게임의 정체를 한 장면에 담는다.** 정비공 게임의 축이
     *   「동시에 두 곳에 못 있는다」인데 지금까지 한 번도 안 켜졌다.
     *   조종석에 사람이 있어야 하는데 **고칠 것은 조종석 밖에 있다.**
     */
    star: true,
  },
  D: {
    key: 'D', name: '잔해밭',
    lead: '전방에 잔해',
    from: 'sky', hands: '비킨다 — 맞으면 부품이 든다',
    uses: ['hazard', 'fix'],
    /**
     * ★ **예고 박자부터** 연다 — D 만 다르다.
     *   잔해는 **제 예고를 이미 갖고 있다** (26~38초 · `hazard-table.js`).
     *   대응부터 열면 예고가 두 번 뜬다 — 장면이 「전방에 잔해」라고 하고
     *   30초 뒤에 또 「전방에 잔해」가 뜬다. 그러면 둘 다 안 읽는다.
     *   **잔해의 예고가 곧 장면의 예고**다.
     */
    opensAt: 'warn',
    built: true,          // game/hazard.js — 이미 돈다
  },
  E: {
    key: 'E', name: '정전',
    lead: '전력 계통 이상',
    // ══ ★★★ v112 — **차단기가 없어졌다. 어둠은 남았다** ═══════════════
    //  통로에 걸어가 차단기를 찾던 장면이었는데 통로가 없어졌다 (v109).
    //  ★ 그런데 **없앨 장면이 아니다** — 이 장면의 값은 차단기가 아니라
    //    **어둠**이고, 어둠은 조종석에서 더 무섭다: 계기가 꺼지면
    //    레이더도 조준경도 없이 **창밖만** 보고 몰아야 한다.
    //  ★★ 되살리는 손은 **수리 단추(P)** 다. 새 손을 안 만든다
    from: 'ship', hands: '계기가 꺼진다 — 창밖만 보고 몬다. P 로 되살린다',
    uses: ['dark', 'fix'],
    built: true,          // game/blackout.js — v56 부터 돈다
  },
  // ══ ★★★ v112 — **F(감압)를 접었다** ═══════════════════════════════
  //
  //  v62 에 우주복이 생기면서 겨우 지은 장면이었다: 미소운석이 뚫은 방이
  //  진공이 되고, 우주복을 입고 문 너머로 들어가 구멍을 메운다.
  //  ★ **그 셋이 다 없어졌다** — 방(v109) · 문(v109) · 우주복(v109).
  //    남는 것이 「기밀 경보가 뜬다」뿐인데, 그건 장면이 아니라 소리다.
  //  ★★ 억지로 살리는 길은 있었다 (조종석이 감압된다 → 우주복). 그런데
  //    그러면 **v109 가 없앤 우주복을 되살려야** 하고, 사장님이 「오직
  //    전투와 파밍」이라고 하신 것을 정면으로 거스른다. **접는다.**
  //  ★ 이 자리는 **J(투하)**가 받는다 — 여덟이 일곱이 되고, 대신 목적
  //    한 줄의 마지막 마디가 처음으로 장면이 된다
  /**
   * ★★ **G 는 줄기 「혼자다」의 가운데다** (7판 · v54 · story-table.js).
   *   그 줄기는 손목 장치(1구간)와 성간 공백(12구간) 둘로만 버티고
   *   있었고 가운데가 비어 있었다 — 2시간 내내 아무도 없다가 마지막에
   *   「아무도 없다」로 끝나면 그건 줄기가 아니라 배경이다.
   *   **2시간에 딱 한 번** 남의 목소리를 넣는다. 한 번이라 무겁다.
   */
  G: {
    key: 'G', name: '구조 신호',
    lead: '조난 신호를 받았습니다',
    // ★ v112 — 에어록에서 하던 일이 아니다. **항로를 트는 결심**이고,
    //   그건 조종석에서 한다 — 방이 없어져도 이 장면은 그대로 산다
    from: 'soul', hands: '가거나 안 간다 — 가면 시간과 자국',
    uses: ['route', 'supply'],
    built: true,          // game/rescue.js — v54 부터 돈다
  },
  // ══ ★★★ v112 — **H(성간 공백)를 접고 J(투하)를 세웠다** ═════════════
  //
  //  ★ H 는 v53 에 지은 마지막 구간이었다 — 「추격도 거점도 과녁도 없는
  //    것」이 장면이고, 앞의 11구간이 얼마나 들고 들어왔나를 청구하는 자리.
  //  ★★ 그런데 **목적이 뒤집혔다** (`WAR.md` · 2026-08-08). 「따라오지
  //    못하는 곳까지 간다」가 「**적 행성에 떨군다**」가 됐고, `CLAUDE.md`
  //    가 「버리는 것은 성간 공백과 따라오지 못하는 곳 둘뿐」이라고 적어
  //    두었다. 그 날이 왔다.
  //  ★★★ 그래서 마지막 구간은 **도망이 아니라 도달**이다. 재미있는 것은
  //    H 가 하던 일(「들고 들어간 만큼」)을 J 가 **그대로 이어받는다**는
  //    것이다 — 탄두를 얼마나 채웠나가 결말 셋을 가른다 (`drop-table.js`).
  //    구조가 안 바뀌고 **방향만 뒤집혔다.**
  /**
   * ★★★ **J — 떨군다** (v93 에 지었고 v112 에 장면이 됐다).
   *
   *   ★ 마디가 넷이다: **무장 · 진입 · 투하 · 이탈** (`drop-table.js PHASE`).
   *     다른 여섯은 「무엇이 오는가」인데 J 는 **「내가 무엇을 하는가」**다 —
   *     2시간에 한 번, 여기서만.
   *   ★★ 결말이 셋이라 **끝 화면이 갈린다.** 그래서 마지막 구간이
   *     「살아서 왔나」가 아니라 **「얼마나 채워서 왔나」**를 묻는다
   */
  J: {
    key: 'J', name: '떨군다',
    lead: '적 행성 — 투하 창이 열렸습니다',
    from: 'place', hands: '무장하고 · 진입하고 · 떨구고 · 빠져나온다',
    uses: ['drop', 'head', 'yoke'],
    /** ★ 예고부터 연다 — 진입각을 잡을 틈이 있어야 한다 */
    opensAt: 'warn',
    built: true,          // game/drop.js — v93 부터 돈다
    star: true,
  },
};

/**
 * 12구간 배치 — **같은 장면이 이어 붙지 않는다** (§5)
 *
 * ★ 왜 표로 두나. 무작위로 뽑으면 반드시 「A 다음에 또 A」가 나오고,
 *   막으려고 규칙을 붙이면 그건 **표를 코드로 쓴 것**이다. 그럴 바에는
 *   표로 둔다 — 눈으로 읽히고, 도구가 셀 수 있고, 고칠 때 한 줄이다.
 *
 * ★ **구간 1 은 비어 있다.** 첫 장면이 12분 뒤에 온다. 그 전에 배를
 *   익힌다 — 가르침 일곱이 도는 자리이기도 하다 (TUTORIAL.md).
 *
 * ★ **A 는 세 번, C 는 두 번.** 같은 장면도 두 번째는 조건이 다르다
 *   (`hard` · `permanent`). 그게 「배운 것을 쓴다」와 「또 이거」의 차이다.
 */
export const PLACEMENT = [
  { leg: 1, scenes: [], note: '배를 익힌다 — 첫 장면은 12분 뒤' },
  { leg: 2, scenes: ['A'], note: '이 게임이 무엇인지' },
  { leg: 3, scenes: ['B'], note: '숨 돌리고 고르는 맛' },
  { leg: 4, scenes: ['A'], hard: true, note: '배운 것을 쓴다' },
  { leg: 5, scenes: ['D'], note: '조종을 처음 쓴다' },
  { leg: 6, scenes: ['C'], note: '2막의 절정' },
  { leg: 7, scenes: ['G'], note: '긴장이 아니라 선택 — 곡선의 골' },
  { leg: 8, scenes: ['E'], note: '어둠 — 완전히 다른 결' },
  // ★★★ v112 — 겹침 하나가 **A+F** 에서 **A+D** 로 바뀌었다.
  //   F(감압)를 접었으므로 (방·문·우주복이 다 없어졌다) 그 자리를
  //   D(잔해밭)가 받는다. 뜻은 오히려 더 맞는다: 「쫓기는 중에 길이
  //   막힌다」가 **「쫓기는 중에 비켜야 한다」**가 되고, 그건 조종석에서
  //   손 하나로 두 가지를 해야 하는 자리다
  { leg: 9, scenes: ['A', 'D'], hard: true, note: '★ 겹침 하나 — 쫓기며 비킨다' },
  { leg: 10, scenes: ['B'], hard: true, note: '여기서 못 벌면 못 떨군다' },
  { leg: 11, scenes: ['C', 'A'], permanent: true, hard: true, note: '★ 겹침 둘 — 이번엔 못 고친다' },
  // ★★★ v112 — H(성간 공백) → **J(투하)**. 마지막 구간이 도망이 아니라
  //   도달이다 (`WAR.md` 「적 행성에 떨군다」)
  { leg: 12, scenes: ['J'], note: '★ 떨군다 — 얼마나 채워 왔나가 결말을 가른다' },
];

/**
 * 목표 숫자 — **도구가 이걸 보고 ✔/✘ 를 찍는다** (§10)
 *
 * ★ 여기 없는 것은 검사도 안 한다. 「긴장감 있나」를 재려면 재는 모양으로
 *   적어야 한다 — 그게 `docs/PLANNING.md` 가 말한 완료 조건이다.
 */
export const GOALS = {
  /** 장면 수 = 구간 수. 하나도 안 비고 하나도 안 남는다 (구간 1 빼고) */
  scenes: 11,
  /** 같은 장면이 **이어 붙는** 횟수 */
  backToBack: 0,
  /** 겹치는 구간 수 — **정확히 둘.** 흔하면 안 무섭다 */
  overlaps: 2,
  /** 장면 사이 간격(분) */
  gap: [6, 12],
  /**
   * ══ ★★★ v112 — **「다른 방」이 「다른 데서 온다」가 됐다** ═══════════
   *
   *  옛 목표는 `distinctRooms: 6` 이었다 — 여덟 장면이 배 안의 서로 다른
   *  방에서 열려야 한다는 것이고, 그때는 그것이 「같은 일의 반복이 아니다」를
   *  재는 좋은 자다. 그런데 **방이 조종석 하나만 남았다** (v109).
   *  그 자를 그대로 두면 영영 1 이 나오고, 검사는 **못 고칠 것을 빨갛게**
   *  들고 있게 된다 — 그런 검사는 며칠 안에 무시된다.
   *
   *  ★★ 그래서 자를 바꾼다: **무엇이 오는가**(`from`). 다섯이다 —
   *    적(foe) · 행성·거점(place) · 내 배(ship) · 하늘(sky) · 사람(soul).
   *    같은 조종석에 앉아 있어도 **적이 붙는 것과 계기가 꺼지는 것은
   *    다른 일**이고, 재고 싶었던 것은 원래 그것이다
   */
  distinctFrom: 4,
};

/**
 * 여운 — ★ **여기가 「시간 가는 줄 모른다」의 자리다** (§2)
 *
 * ★ 만들어 놓고 보니 **여운이 비어 있었다.** 40~90초 동안 장면이 아무것도
 *   예약하지 않고 잔일 문만 열어 뒀는데, 고장 시계는 95~165초라
 *   **여운 안에 아무 일도 안 일어나는 경우가 흔했다.**
 *   그러면 「손이 쉬는 시간 12초 이하」(§10)가 그 자리에서 깨진다.
 *
 * ★ 그리고 오는 것은 **아무 고장이나가 아니라 쌓인 마모**에서 나온다.
 *   장면 동안 부딪히고 무리한 만큼이 여운에서 일로 돌아온다 —
 *   `drift-table.js` 가 적어 둔 **「벌은 미뤄지지 없어지지 않는다」**가
 *   실제로 지켜지는 자리가 여기다. 안 그러면 그 문장은 주석일 뿐이다.
 */
export const EMBER = {
  /** 여운에 일이 하나 온다 */
  chore: true,
  /** 온 것은 쌓인 마모에서 나온다 — 무작위가 아니다 */
  fromWear: true,
  /**
   * 여운이 시작되고 이 안에 온다 (초).
   * ★ 0 이면 안 된다 — 해소(「됐다」) 직후 3초는 **이 게임 최고의 자산**이라
   *   그 위에 바로 일을 얹으면 보상이 사라진다 (`ESCAPE` · PLAN §6)
   */
  within: 12,
  /** 이만큼은 닳아 있어야 일이 된다. 멀쩡한데 일을 내면 그건 벌이 아니라 소음 */
  needWear: 0.12,
};

/** 이 구간에 오는 장면들 (1부터 센다) */
export function scenesAt(leg) {
  const row = PLACEMENT.find((r) => r.leg === leg);
  return (row?.scenes ?? []).map((k) => ({ ...SCENES[k], ...row, scenes: undefined }));
}

/** 지금 게임에 **정말 있는** 장면만 — 없는 것은 배치해 둬도 안 뜬다 */
export const builtKeys = () => Object.keys(SCENES).filter((k) => SCENES[k].built);

/**
 * 배치가 규칙을 지키나 — **도구와 게임이 같은 함수를 본다.**
 * ★ 검사용으로 따로 세면 검사만 맞고 게임은 틀린 상태가 생긴다.
 */
export function audit(rows = PLACEMENT) {
  const filled = rows.filter((r) => r.scenes.length);
  let backToBack = 0;
  for (let i = 1; i < rows.length; i++) {
    const a = rows[i - 1].scenes, b = rows[i].scenes;
    if (a.some((k) => b.includes(k))) backToBack++;
  }
  // ★ v112 — 「방이 몇 가지」에서 **「오는 데가 몇 가지」**로 (위 GOALS)
  const rooms = new Set(filled.flatMap((r) => r.scenes).map((k) => SCENES[k]?.from));
  return {
    scenes: filled.length,
    overlaps: rows.filter((r) => r.scenes.length > 1).length,
    backToBack,
    /** ★ v112 — 「방이 몇 가지」에서 **「오는 데가 몇 가지」**로 (위 GOALS) */
    rooms: rooms.size, from: rooms.size,
    /** 몇 번씩 나오나 */
    counts: rows.flatMap((r) => r.scenes).reduce((m, k) => ({ ...m, [k]: (m[k] ?? 0) + 1 }), {}),
    /** 아직 안 만든 것 — **배치는 해 뒀지만 안 뜨는 장면** */
    missing: [...new Set(rows.flatMap((r) => r.scenes))].filter((k) => !SCENES[k]?.built),
  };
}
