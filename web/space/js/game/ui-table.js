// ══════════════════════════════════════════════════════════════════════════
//  ★★★ **UI 크기** — 한 곳에서 키운다 (v135)
//
//  ★ 사장님 (2026-08-13) 「글씨가 겹쳐서 구분이 안되잔아.
//    **UI를 전체적으로 크기를 키워. 전부**」
//
//  ══ ★★★ 겹친 것과 작은 것은 **다른 일**이었다 ═══════════════════════════
//
//  겹친 것은 크기 탓이 아니라 **판 둘이 같은 자리에 그려진** 것이었다 —
//  상태창 여덟 줄 위에 획득 목록이 그대로 얹혀 있었다 (`world/status.js`
//  의 `drawTook`). 그건 거기서 없앴다. **작은 것은 따로 고친다** —
//  섞어서 「키웠더니 안 겹치더라」로 끝내면 다음에 또 얹힌다.
//
//  ══ ★★ 화면에 글이 뜨는 자리가 **둘**이다 ═══════════════════════════════
//
//    ① **DOM** — 조준선 · 배너 · 손목 · 툴팁 · 베이 · 범례 (CSS · px)
//    ② **3D 판** — 상태창 · 레이더 · 광학창 (배에 박힌 메시 · 미터)
//
//  ★★★ **둘 다 키워야 「전부」다.** 한쪽만 키우면 나머지가 상대적으로
//    더 작아 보여서 오히려 어긋난다. 그래서 배율을 **한 곳**에 두고
//    둘이 같이 읽는다 — 「재는 곳을 둘로 만들지 않는다」.
//
//  ★ three.js 를 안 쓴다 — `tools/space-ui.js` 와 `space-screen.js` 가 읽는다.
// ══════════════════════════════════════════════════════════════════════════

export const UI = {
  /**
   * ★★★ **DOM 배율** — CSS `zoom` 으로 한 번에 건다 (`body > *:not(#view)`).
   *
   *   ★ 왜 `zoom` 인가. 이 배의 CSS 는 글자·여백·테두리가 다 **px** 이고
   *     서른 곳이 넘는다. 하나씩 `calc()` 로 바꾸면 **바꾸다 만 자리가
   *     반드시 남고**, 그 자리만 안 커진 채 배포된다 (이 저장소가 옛
   *     2D·3D 시절에 정확히 그렇게 뎄다). `zoom` 은 **레이아웃째** 키우므로
   *     빠뜨릴 자리가 없다.
   *   ★★ 3D 캔버스(`#view`)에는 안 건다 — 걸면 렌더 해상도가 깎인다.
   *
   *   ★ 1.30 은 「한 눈금 크게」다. 1.6 이면 좁은 창에서 베이·툴팁이
   *     화면을 넘치고, 1.15 면 사장님이 겪으신 것이 그대로 남는다.
   *   ★★ **처음에 1.35 · 판 1.9 로 뒀다가 검사가 잡았다** — 그런데 그때
   *     검사가 **틀린 것을 재고 있었다.** `PANELS[].area` 는 「이만큼까지는
   *     커도 된다」는 **상한**이지 실제 크기가 아닌데, 그 상한을 예산과
   *     견주고 있었다. **진짜 크기는 카메라에 투영해서** 나온다
   *     (`space-screen.js`) — 재 보니 15.5% 였고 예산 29% 에 한참 못 미쳤다.
   *   ★★★ 그래서 **더 올렸다.** 「검사가 빨개졌으니 줄인다」로 끝냈으면
   *     사장님이 「키우라」고 하신 것을 **절반만** 하고 넘어갔을 것이다.
   *   ★★★ 다만 2.2 까지 올렸더니 **4:3 창에서 광학창이 전투 원뿔로
   *     들어왔다** — 뼈대 셈은 통과했는데 **진짜 투영**이 잡았다
   *     (`space-screen.js`). 뼈대는 aspect 를 모르고, 세로로 긴 창에서는
   *     같은 미터가 더 큰 화면 좌표가 된다. **한 창만 재면 절반**이라는
   *     v114 의 교훈이 여기서 또 나왔다. 그래서 1.95 로 한 눈금 내렸다
   */
  zoom: 1.38,

  /**
   * ★★★ **3D 계기 배율** — 상태창·레이더·광학창이 하늘에서 차지하는 넓이.
   *
   *   ★ 이것은 `screen-table.js` 의 `PANELS[].area` 와 `BUDGET` 에 곱해진다.
   *     넓이 배율이므로 **글자 크기로는 √1.9 ≈ 1.38 배**다 — 위 `zoom`
   *     과 눈에 비슷하게 맞춘 값이다 (한쪽만 커 보이면 안 맞춘 것이다).
   *
   *   ★★ **전투 원뿔(`CONE` 0.45)은 안 건드린다.** v103 에 사장님이
   *     「전투화면이 더 광활하게」라고 하셨고, 판이 커지면서 그 안으로
   *     들어오면 **키운 것이 아니라 가린 것**이 된다. 그래서 판은
   *     **구석에 붙은 채로** 커진다 — `space-screen.js` 가 되짚는다
   */
  //  ══ ★★★ v139 — **화각을 좁히면 계기가 저절로 커진다** ═══════════════
  //   화각 94 → 74 로 좁히자 `space-screen.js` 가 **「전투 원뿔에 셋 다
  //   들어왔다」**로 그 자리에서 잡았다. 판 크기는 **미터**로 적혀 있는데
  //   화각이 좁아지면 같은 미터가 **화면을 더 먹기** 때문이다:
  //       화면 몫 배수 = (94/74)² = 1.61
  //   ★★ 그래서 1.95 ÷ 1.61 ≈ **1.21** 로 내린다 — 사장님이 v135 에
  //     「키우라」고 하신 크기는 **화면에서** 그대로 유지된다. 미터를
  //     지키는 것이 아니라 **보이는 크기**를 지키는 것이 맞다.
  //   ★ v138 에서 「화각을 건드리면 그 둘을 같이 다시 재야 한다」고 적어
  //     둔 그 자리다. 적어 뒀고, 정말 그랬다
  //  ★★ 0.97 에서 한 눈금 더 내렸다 — 4:3 창에서 **조준경과 광학창이
  //    겹쳤다.** 조준경은 40도라(v121) 화각이 좁아지면 화면에서 크게
  //    자라는데, 광학창은 그 옆 구석에 있다. 겹치면 **둘 다 못 읽는다**
  //  ★ v140 — 화각이 86 으로 돌아오면서 판도 되돌린다 (1.95 × (86/94)²)
  panel: 1.63,

  /**
   * ★ 3D 계기가 합쳐서 화면의 몇 할까지 — `screen-table.js BUDGET` 의 새 값.
   *   ★★ 14% 는 v103 이 「전투화면을 넓게」로 **조인** 값이다. 지금은
   *     사장님이 **키우라**고 하셨으므로 푼다. 다만 원뿔은 그대로이므로
   *     넓어지는 것은 **구석**이지 복판이 아니다
   */
  budget: 0.29,
};

/** 넓이 배율 → 길이 배율 (판의 가로·세로에 곱한다) */
export const panelSide = () => Math.sqrt(UI.panel);

/**
 * ★★★ v139 — **화면에서 몇 배로 보이나** (미터가 아니라).
 *
 *   ★ v138 까지 「계기가 커졌나」를 **미터 배율**로 물었다. 그런데 v139 가
 *     화각을 94 → 74 로 좁히자 **같은 미터가 화면을 1.27배 더 먹게** 됐다 —
 *     미터를 그대로 두면 화면에서는 커지고, 미터를 줄여도 화면에서는
 *     안 줄어들 수 있다. **묻는 것이 틀렸던 것이다.**
 *   ★★ 사장님이 v135 에 「키우라」고 하신 것은 **화면에서 크게**이지
 *     「미터로 크게」가 아니다. 그래서 여기서 화각을 같이 셈한다.
 *   ★ 기준은 v134 의 94도다 — 그때가 「키우기 전」이고, 배율은 그 대비다
 */
export const FOV0 = 94;
export const screenSide = (fov = FOV0) => panelSide() * (FOV0 / fov);
