// ══════════════════════════════════════════════════════════════════════════
//  G — 구조 신호 · **2시간에 한 번뿐인 남의 목소리** (7판 · story-table.js)
//
//  ★ 왜 이 장면이 필요한가 — 줄기 「혼자다」가 제일 얇았다
//    `story-table.js` 를 짜고 도구를 돌리자 바로 나왔다: 「혼자다」는
//    손목 장치(1구간)와 성간 공백(12구간) **둘로만** 버티고 있었고
//    가운데가 비어 있었다. 2시간 내내 아무도 없다가 마지막에 「아무도
//    없다」로 끝나면 그건 줄기가 아니라 **배경**이다.
//
//    그래서 **딱 한 번** 남의 목소리를 넣는다. 한 번이라 무겁다.
//
//  ★★ 재고 나서 설계가 한 번 뒤집혔다 (2026-08-06)
//    처음에는 「응답하면 자국이 **확** 오른다」로 짜려 했다. 재 보니
//    **일회성 자국은 벌이 아니었다** — `SIGN.riskFall` 이 2.2/초라
//    +22 를 얹어도 **10초면 씻긴다.** 벌처럼 보이는데 벌이 아니다.
//
//    이 게임에서 밖에 대고 뭔가 하는 동안의 벌은 이미 모양이 있다 —
//    **윈치**다: `WINCH.sign` 9/초를 50초 동안 얹고, 그것이
//    「한 통에 접촉 위험 26%」로 재져 있다 (`tools/space-supply.js`).
//    구조 신호도 **같은 모양**이어야 한다: 켜 두는 동안 계속 오른다.
//
//  ★ 그리고 **새 동사를 안 만든다**
//    무전을 받으면 그들이 옆에 붙고, **바깥문을 열어 받는다** —
//    에어록·바깥문·윈치가 그대로 다시 쓰인다. 새 계통이 아니라
//    **있던 동작에 새 이유**다. 그래야 배우는 것이 하나도 안 는다.
//
//  ★ three.js 를 안 쓴다 — tools/space-rescue.js 가 브라우저 없이 읽는다.
// ══════════════════════════════════════════════════════════════════════════

export const RESCUE = {
  /**
   * 무전을 잡고 있어야 하는 시간 (초).
   *
   * ★ 윈치 한 통이 50초다. 그보다 **짧게** 잡는다 — 대신 자국이 세다.
   *   길면 「또 붙들고 있기」가 되고, 이 장면은 2시간에 한 번뿐이라
   *   **기억에 남을 만큼만** 길면 된다.
   */
  answer: 42,
  /**
   * 응답하는 동안 초당 자국. **윈치(9)보다 세다** — 무전은 방송이다.
   *
   * ★ 「우주에서 스텔스는 불가능하다」가 이 게임의 전제이고, 무전은
   *   그 전제를 제일 크게 어기는 짓이다. 도와주려고 켠 것이 나를 가리킨다 —
   *   줄기 「혼자다」와 「쫓긴다」가 **여기서 만난다**.
   */
  sign: 13,
  /**
   * ★ 응답하는 동안 **구간이 안 나아간다** (착륙의 `hold` 와 같은 규약).
   *   다가가느라 시간을 버리는 것이고, 그동안 압박은 계속 쌓인다.
   *   이걸 빼면 「공짜로 얻는 것」이 되어 안 갈 이유가 없어진다.
   */
  hold: true,

  /**
   * 붙은 뒤 **바깥문을 열고 받는 시간** (초).
   * 짧다 — 여기서 또 오래 붙들면 장면 하나가 두 번 길어진다.
   */
  take: 14,
  /**
   * 붙어 있어 주는 시간 (초). 이 안에 바깥문을 안 열면 **떠난다.**
   * ★ 넉넉해야 한다 — 「받는 법을 몰라서 놓쳤다」는 벌이 아니라 함정이다.
   */
  waitFor: 75,

  /**
   * 받는 것. **광석은 안 준다** — 광석은 내가 캐는 것이다 (PLAN §5-3).
   *
   * ★ 부품이 열쇠다. 거점 거래가 한 번에 부품 1 이고 창고가 12 이므로,
   *   부품 2 는 **거래 두 번어치**다. 그게 「갈 만하다」의 크기다.
   */
  gives: { parts: 2, food: 30 },

  /** 예고에서 대응으로 넘어가기 전, 신호가 잡히는 자리 */
  room: 'airlock',

  // ── 말 ────────────────────────────────────────────────
  /** 예고 — 아직 응답하지 않았다 */
  heard: '조난 신호를 받았습니다 — 에어록 무전기',
  /** 잡고 있는 동안 */
  answering: '응답하는 중 — 무전은 방송입니다',
  /** 다 응답했다 */
  near: '옆에 붙었습니다 — 바깥문을 여십시오',
  /** 받았다 */
  took: '받았습니다',
  /** 안 받고 놓쳤다 */
  left: '떠났습니다',
  /** 아예 응답 안 했다 */
  passed: '신호가 끊겼습니다',
};

/** 지금 무엇을 하는 중인가 */
export const RSTEP = {
  NONE: 'none',
  /** 신호가 온다 — 무전기가 잡힌다 */
  HEARD: 'heard',
  /** 잡고 있다 */
  ANSWERING: 'answering',
  /** 옆에 붙었다 — 바깥문을 열면 받는다 */
  NEAR: 'near',
  /** 받았다 */
  TOOK: 'took',
  /** 지나쳤다 (응답 안 함) 또는 놓쳤다 (붙었는데 안 받음) */
  MISSED: 'missed',
};

/** 끝 화면이 읽는 말 — **가고 안 가고가 목록에 남는다** */
export const RESCUE_WORD = {
  [RSTEP.TOOK]: '구조 신호에 응답했다',
  [RSTEP.MISSED]: '구조 신호를 지나쳤다',
};
