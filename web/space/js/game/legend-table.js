// ══════════════════════════════════════════════════════════════════════════
//  ★★★ **범례** — 저게 적인가 물건인가 (v108). 숫자만.
//
//  ★ 사장님 (2026-08-11)
//    「**우측 화면 상단에 도형이나 기타 표시로 적인지 물체인디 행성인지
//      알 수 있도록 도움말 같은걸 표시**해줘. 그래서 **글씨가 아닌
//      이모티콘이나 도형으로 무슨 의미인지** 알 수 있도록」
//
//  ══ 재 보니 **모양은 아홉인데 무리가 없었다** ═══════════════════════════
//
//  조준경은 이미 종류마다 다른 모양을 그린다 (`gunsight.js glyph`) —
//  아홉 가지다. 그런데 **무리가 없었다**: 쐐기가 셋(적 우주선·요격기·
//  포함)이고 마름모가 자폭정인데, 처음 보는 사람에게 「쐐기 = 적」이라고
//  말해 주는 것이 아무 데도 없었다. 색도 「사거리 안/밖」에만 쓰였다.
//
//  ★★★ 그래서 **무리를 셋으로** 묶고 **색을 무리에 준다**:
//
//      ✕ 빨강 — **적** (쏜다 · 들이받는다). 다섯
//      ◆ 초록 — **물건** (부수면 나온다). 셋
//      ● 파랑 — **행성·거점** (들를 수 있다). 하나
//
//  ★ 셋인 것은 사장님이 말씀하신 그대로다 (「적인지 물체인디 행성인지」).
//    넷째를 넣고 싶어지면 하나를 접는다 — 색을 늘리면 그때부터 **색을
//    배우는 게임**이 된다 (이 저장소의 오랜 규약: 계기가 셋으로만 말한다).
//
//  ★★ **글씨가 아니라 모양**이 뜻을 진다. 범례에 있는 한 마디는
//    「처음 한 번 읽는 열쇠」이고, 그 뒤로는 하늘에서 모양만 본다
//
//  ★ three.js 를 안 쓴다 — `tools/space-legend.js` 가 읽는다.
// ══════════════════════════════════════════════════════════════════════════

/**
 * ★★★ 무리 셋. `mark` 는 **도형 이름**이고 `hue` 는 그 무리의 색이다.
 *
 *   ★ 색은 이 배가 이미 쓰는 것에서 가져왔다 — 새 색을 안 만든다.
 *     빨강은 피격·경고, 초록은 「쏠 수 있다」, 파랑은 레이더 계기다
 */
export const BANDS = [
  {
    key: 'foe', name: '적', mark: 'wedge', hue: '#ff6a52',
    what: '쏘거나 들이받는다 — 먼저 부수거나 피한다',
  },
  {
    key: 'thing', name: '물건', mark: 'chip', hue: '#7fe6a8',
    what: '부수면 재료가 나온다 — 파츠·광석·부품',
  },
  {
    key: 'place', name: '행성 · 거점', mark: 'disc', hue: '#6ab6ff',
    what: '들를 수 있다 — 내려서 가져오거나 산다',
  },
];

export const BAND_BY = Object.fromEntries(BANDS.map((b) => [b.key, b]));

/**
 * ★★ 종류마다 어느 무리인가.
 *
 *   ★ **`KINDS` 를 보고 정한다**가 아니라 여기 적는다. 「쏘면 적」으로
 *     자동으로 가르면 보급 호송선(안 쏜다)이 물건이 되고, 자폭정
 *     (안 쏘고 들이받는다)이 물건이 된다 — 둘 다 틀렸다
 */
export const BAND_OF = {
  raider: 'foe', fighter: 'foe', gunship: 'foe', drone: 'foe', turret: 'foe',
  // ★ v117 — 얼음 덩어리·표류선도 **물건**이다 (부수면 재료가 나온다).
  //   검사가 「빠진 것 ice · hulk」로 잡아 줬다 — 무리에 안 들면 창밖에서
  //   **무슨 색 무슨 도형으로 그릴지**가 정해지지 않는다
  junk: 'thing', sat: 'thing', tank: 'thing', convoy: 'thing',
  ice: 'thing', hulk: 'thing',
  planet: 'place', port: 'place',
};

/** 이 종류가 어느 무리인가 — 모르면 **물건**으로 본다 (안전한 쪽) */
export const bandOf = (kind) => BAND_BY[BAND_OF[kind] ?? 'thing'];

/** 그 종류를 무슨 색으로 그리나 */
export const hueOf = (kind) => bandOf(kind).hue;

/**
 * ★ 범례가 화면 어디에 앉나 (0~1 · 오른쪽 위).
 *   사장님 「**우측 화면 상단**」 그대로다
 */
export const AT = { right: 0.015, top: 0.015, w: 0.19 };

/**
 * ★★ **처음 얼마 동안만 한 마디를 같이 보여 준다** (초).
 *   그 뒤로는 **도형만** 남는다 — 사장님 「글씨가 아닌 … 도형으로」.
 *   0 으로 두면 영영 글씨가 붙어 있고, 그건 범례가 아니라 설명서다
 */
export const WORDS_FOR = 90;
